# Action-QBC v8 P8v6 runner-manifest-key recovery preregistration

Correction freeze: 18 August 2026 (Australia/Sydney)

Status: prospective preregistration-only administrative recovery. O8v2 was
constructed, frozen, published, made PUBLIC, and successfully verified by its
sole credential-disabled anonymous two-ref query before this document. No O8v2
bootstrap command, v8 execution root, preparation command or receipt, Windows
claim or receipt, Linux lifecycle command, scientific start, scientific import,
payload, finalization bundle, R8 object, result branch, or result tag existed
when this document was written.

## 1. Authority, incorporation, and immutable history

This document corrects and otherwise incorporates the complete protocol through:

    docs/experiment_amendment_2026-08-18_action_qbc_v8_open_bounded_remote_verification_v5_public_visibility_recovery.md

Every P8v5 scientific, schema, path, timing, resource, claim, absence,
no-cleanup, no-retry, evidence, failure-classification, and result-publication
requirement remains binding unless this document expressly replaces an
administrative lineage, ref, public-state, source-manifest-key, tool-identity, or
command-plan value. P8v5 is historical authority; it is not rewritten.

The binding P8v4 commit is:

    e0bff9ffc185196cafa938c8f7c9a7186366258b

Its immutable lightweight tag is:

    prereg-action-qbc-v8-open-bounded-remote-verification-v4

O8v1 is P8v4's direct exact-fifteen-addition child:

    commit: 7685fbdccd41702216b3a3f06d2a0ac699aca7ec
    tree:   9b9ad5ba986afacbcdb1fde3cd69e0f1c94efdf2
    tag:    action-qbc-v8-open-diagnostic-freeze-v1

P8v5 is O8v1's direct child:

    commit:              09f9caea346866a1acf35c20e0c9d937096b5ce3
    tree:                47a978cdd887fd6dc1cb5e80e36aa3e0a5a29253
    tag:                 prereg-action-qbc-v8-open-bounded-remote-verification-v5
    document:            docs/experiment_amendment_2026-08-18_action_qbc_v8_open_bounded_remote_verification_v5_public_visibility_recovery.md
    document Git blob:   7c0955a775af89dcfcde4796a9bbb4d470669d10
    document SHA-256:    cc9d787a64700332a44f543e7a949ee5522c3663b6b0eb54e418840e560cfe6d
    document byte count: 25872

Its direct O8v1-to-P8v5 diff is exactly fifteen deletions plus the one P8v5
recovery-document addition. Its net P8v4-to-P8v5 diff is exactly that one
addition.

O8v2 is P8v5's direct exact-fifteen-addition child:

    commit: 8da637a47de0c88f917f222e52e54b342d729be9
    tree:   247eba59e1e2ac9b0611c0e361de945dae0f2dc8
    tag:    action-qbc-v8-open-diagnostic-freeze-v2

The existing lineage branch is:

    action-qbc-v8-prereg

At this correction freeze that branch and freeze-v2 both resolve to O8v2;
freeze-v1 remains at O8v1 and the v5 preregistration tag remains at P8v5. All
three historical tags are immutable. No operator may move, replace, delete,
force-update, bootstrap, execute, or anonymously reverify O8v1 or O8v2.

P8v6 must be the direct child of O8v2. Its direct diff deletes exactly the same
fifteen paths O8v2 added and adds only this recovery document:

    docs/experiment_amendment_2026-08-18_action_qbc_v8_open_bounded_remote_verification_v6_runner_manifest_key_recovery.md

Consequently P8v6's tree must equal the P8v5 tree plus this one document by
path, mode, Git blob, raw SHA-256, and byte count. The net P8v5-to-P8v6 diff is
exactly the one P8v6 document addition. P8v6 modifies, deletes, renames, or adds
no other path. Its immutable lightweight tag is:

    prereg-action-qbc-v8-open-bounded-remote-verification-v6

P8v6_COMMIT, P8v6_TREE, P8v6_DOCUMENT_GIT_BLOB_SHA1,
P8v6_DOCUMENT_SHA256, and P8v6_DOCUMENT_BYTE_COUNT mean identities derived
from the committed bytes after P8v6 exists. They are not operator choices.

O8v3 must be P8v6's direct child and add exactly the fifteen paths in section 8.
It may modify, delete, or rename no P8v6 path. Its immutable lightweight tag is:

    action-qbc-v8-open-diagnostic-freeze-v3

The result branch retains its unversioned alias:

    action-qbc-v8-open-diagnostic-result

The new immutable result tag is:

    action-qbc-v8-open-diagnostic-result-v3

The v1 and v2 result tags remain reserved to their historical lineages and must
remain absent:

    action-qbc-v8-open-diagnostic-result-v1
    action-qbc-v8-open-diagnostic-result-v2

The linear lineage is:

    R7 -> P8v1 -> P8v2 -> P8v3 -> P8v4 -> O8v1 -> P8v5 -> O8v2 -> P8v6 -> O8v3 -> R8v3

R8v3 exists only if the incorporated one-shot lifecycle reaches its registered
result-publication boundary.

## 2. Complete historical O8v1 disclosure

P8v5's complete failed-O8v1 disclosure remains incorporated. O8v1 construction,
frozen gates, commit creation, lightweight tagging, and one non-force atomic
publication push succeeded. Its sole credential-disabled anonymous query then
returned exit code 128 because the repository was PRIVATE. It produced zero
stdout bytes and the exact ASCII stderr:

    fatal: could not read Username for 'https://github.com': terminal prompts disabled\n

That stderr was 83 bytes, ended in LF, and had SHA-256:

    2e554a39e2bb4414b5e2c3f5a90ebc87b405b5b8231d692704ec6df4580e8f60

The empty stdout SHA-256 was:

    e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855

That invocation consumed O8v1's check and stopped before bootstrap. No O8v1
authority root, claim, preparation, lifecycle, scientific observation, result,
or result ref exists. O8v1 remains immutable administrative history.

## 3. Complete O8v2 construction and publication disclosure

O8v2 was constructed from P8v5 with exactly the registered fifteen additions.
Its generated registration had these identities:

    path:           artifacts/action_qbc_v8_open_registration.json
    byte count:     209576
    final LF:       absent
    file SHA-256:   7f86b2999697992ba4a803dc1b00e87dbd7d8d10c6e7759213ef15282cbcf106
    content SHA-256: 01172ae8056fd6df820830db2c6f4047af786ac72a81abbbcd629b1a8e4f11de
    Git blob:        c11a8578437315a367b2f9f41fb3806000a0eeac
    source manifest: 503a296cb5d9ac494b39a9aba7201eb941499ce26320f8723be91695da4bb1ab

The producer, byte reconstruction, frozen pytest, Ruff, strict-mypy, source,
tree, index, and post-commit gates succeeded before the O8v2 tag and publication.
The exact one-shot non-force atomic Windows Git push was:

    ["C:\\Users\\User\\anaconda3\\Library\\bin\\git.exe","--no-replace-objects","--no-optional-locks","-C","D:\\kaggle competitions\\arc3-crosslevel-voi","push","--atomic","origin","refs/heads/action-qbc-v8-prereg:refs/heads/action-qbc-v8-prereg","refs/tags/action-qbc-v8-open-diagnostic-freeze-v2:refs/tags/action-qbc-v8-open-diagnostic-freeze-v2"]

It returned exit code 0. The captured combined presentation was:

    To https://github.com/bansarinejad/arc3-crosslevel-voi.git
       09f9cae..8da637a  action-qbc-v8-prereg -> action-qbc-v8-prereg
     * [new tag]         action-qbc-v8-open-diagnostic-freeze-v2 -> action-qbc-v8-open-diagnostic-freeze-v2

The capture did not preserve stdout-versus-stderr attribution or a raw combined
stream hash. This document makes no unsupported assertion about either.

After that successful push, the sole authorized visibility mutation was invoked:

    ["C:\\Program Files\\GitHub CLI\\gh.exe","repo","edit","bansarinejad/arc3-crosslevel-voi","--visibility","public","--accept-visibility-change-consequences"]

It returned exit code 0; no output was captured. An earlier sandbox approval
rejection did not launch GitHub CLI, perform an API request, or count as a
protocol invocation. The successful command was the sole PRIVATE-to-PUBLIC
transition. The repository has remained PUBLIC.

The next protocol network action was the sole credential-disabled literal-HTTPS
O8v2 query from the registered neutral WSL cwd and empty environment:

    ["C:\\Windows\\System32\\wsl.exe","-d","Ubuntu","--cd","D:\\kaggle competitions","--","/usr/bin/env","-i","GIT_CONFIG_COUNT=0","GIT_CONFIG_GLOBAL=/dev/null","GIT_CONFIG_NOSYSTEM=1","GIT_NO_REPLACE_OBJECTS=1","GIT_TERMINAL_PROMPT=0","HOME=/home/bansarinejad","LANG=C","LC_ALL=C","PATH=/usr/local/bin:/usr/bin:/bin","XDG_CONFIG_HOME=/nonexistent","/usr/bin/git","--no-replace-objects","-c","credential.interactive=never","-c","core.askPass=","-c","credential.helper=","ls-remote","--refs","https://github.com/bansarinejad/arc3-crosslevel-voi.git","refs/heads/action-qbc-v8-prereg","refs/tags/action-qbc-v8-open-diagnostic-freeze-v2"]

It returned exit code 0 and empty stderr. Stderr was zero bytes with SHA-256:

    e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855

Its stdout was exactly these two LF-terminated ASCII records, in the observed
presentation order:

    8da637a47de0c88f917f222e52e54b342d729be9	refs/heads/action-qbc-v8-prereg
    8da637a47de0c88f917f222e52e54b342d729be9	refs/tags/action-qbc-v8-open-diagnostic-freeze-v2

That stdout was 164 bytes with SHA-256:

    f83b6bd5dfe2dd611a8150559700dd34a3e492c34e14e119d155810c00bdd568

It contained each requested ref exactly once at O8v2, with no additional,
peeled, duplicate, malformed, or authenticated record. The invocation consumed
O8v2's anonymous check successfully. It may never be repeated or treated as the
prospective O8v3 check.

## 4. Exact post-verification stop state

Read-only audits after O8v2's successful public verification discovered the
defect in section 5 before any bootstrap command was invoked. At that point and
at this correction freeze:

- `/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open` was absent;
- the Windows remote-verification claim, verifier-start claim, remote receipt,
  and supervisor receipt paths under `D:\kaggle competitions` were absent;
- no preparation or preparation-verification command or receipt existed;
- no supervisor, execution-phase verifier, lifecycle, arm, driver, scientific
  runner, payload validator, finalizer, publisher, or transfer command ran;
- no scientific module was imported and no private scene, extension scene,
  process A, process B, payload, result bundle, R8 object, or v8 scientific
  observation existed; and
- the stable result branch and both result-v1 and result-v2 tags were absent.

No cleanup or repair was performed or required. The O8v2 registration and
freeze remain immutable evidence of a successfully published but unexecuted
administrative implementation.

## 5. Frozen defect, correction, and causal boundary

The canonical O8v2 registration `source_manifest` contains exactly the keys
`manifest_sha256`, `open_freeze_added_files`, and `preregistration_tree`. The
reconstructor writes and validates `open_freeze_added_files`.

The O8v2 runner instead read:

    source_manifest.get("added_in_open_freeze")

That non-schema key is absent. Passing the real O8v2 registration to the
runner's reconstructed-execution validator would therefore deterministically
produce the exact administrative failure message:

    registration open-freeze additions are invalid

The failure occurs during repository verification, before a process start claim
and before scientific import. Had O8v2 been bootstrapped, this defect would have
forced process A to terminate administratively without science. That
counterfactual was not executed.

O8v3 authorizes exactly this runtime correction in
`scripts/run_action_qbc_v8_open_diagnostic.py`:

    source_manifest.get("added_in_open_freeze")

becomes:

    source_manifest.get("open_freeze_added_files")

No compatibility alias, fallback spelling, schema migration, retry, inferred
value, or optional acceptance is allowed. The incorrect key must be rejected.
The canonical key must be tested using the real generated registration shape,
not a hand-built fixture carrying the incorrect name.

Every other executable difference from O8v2 must be an administrative identity,
ancestry, ref, corrected Linux Git identity, regenerated manifest/hash, runbook,
or required regression-test consequence declared here. Any other change requires
another preregistration.

## 6. Corrected Linux Git tool identity

O8v2 registered `/usr/bin/git` as version `2.43.0` with SHA-256:

    953577d782b6a4dada93cdb924a1261266c7b98aae6676e4ddeeddfc9a848e8e

The final read-only pre-bootstrap audit at `2026-08-18T08:36:08Z` established
the current executable's exact identity as:

    path:                  /usr/bin/git
    realpath:              /usr/bin/git
    file type:             regular file
    mode:                  0755
    owner / uid:           root / 0
    group / gid:           root / 0
    device:                2080
    inode:                 37181
    byte count:            4066232
    mtime epoch seconds:   1751487822
    mtime local:           2025-07-03 06:23:42.000000000 +1000
    ctime local:           2026-08-12 06:07:02.790013532 +1000
    SHA-256:               2a8c18fbf43da9f692d75474c72bea9dfd796c260b0f3dfe456376abc3bbd668
    version stdout:        git version 2.43.0\n
    package query stdout:  git\t1:2.43.0-1ubuntu7.3\n

O8v3 must register and enforce the corrected SHA-256 while retaining path
`/usr/bin/git` and semantic version `2.43.0`. The stat and package fields above
are provenance for the correction; they do not expand the existing registration
schema.

The other six incorporated Linux tool identities remain byte-for-byte and
field-for-field unchanged:

    /usr/bin/env       1490a663e7312c4347987b2e12d7d73950ed1e9a322449daf8e4836660396e31  GNU coreutils 9.4
    /usr/bin/install   b4663b43190ea551f682cfac9500f3f4f6e94890d8ce8822bb81a819f15dab00  GNU coreutils 9.4
    /usr/bin/python3   1643dacd9feaedc58f3cc581e4d22577dfe25c09b10282936186ccf0f2e61118  CPython 3.12.3
    /usr/bin/test      52b0ca5cef7e104ad5e0a8a29bd1522c205cc8404e46e153e5afc54605857c4d  GNU coreutils 9.4
    /usr/bin/timeout   2ee918a5358c0388719e710134bc32cffb934f4bd2a8fb9beb86ef4d6ec8bd8a  GNU coreutils 9.4
    /usr/local/bin/uv  1cb9cd0a1749debf6049d7d2bb933882cc52d81016326ee6d99a786d6c988b03  0.11.28

All four incorporated Windows executable identities, the WSL launcher identity, Ubuntu
release field, kernel field, Python environment identity, dependency version,
repository-local config identity, and info/exclude identity remain unchanged.
No tool was installed, upgraded, replaced, or repaired under this recovery.

Any mismatch against any O8v3 registered tool identity is a pre-science stop;
it is not repaired, accepted, or retried.

## 7. Scope, schemas, paths, and scientific invariance

The registration schema remains:

    action-qbc-v8-open-registration-v1

All incorporated receipt, claim, payload, bundle, and result-document schema
names and exact key sets remain unchanged, including:

    action-qbc-v8-preparation-receipt-v2
    action-qbc-v8-preparation-verification-receipt-v1
    action-qbc-v8-remote-tag-verification-claim-v1
    action-qbc-v8-remote-tag-verifier-start-claim-v1
    action-qbc-v8-remote-tag-verification-receipt-v1
    action-qbc-v8-remote-tag-verification-supervisor-receipt-v1
    action-qbc-v8-arm-receipt-v2
    action-qbc-v8-open-diagnostic-payload-v1
    action-qbc-v8-open-diagnostic-receipt-v2
    action-qbc-v8-open-diagnostic-administrative-terminal-v2
    action-qbc-v8-emergency-result-bundle-v2

The treatment, diagnostic, runtime, platform, comparison, transform, resource,
and scientific-contract identifiers remain unchanged. The exact 70-key
execution contract remains unchanged. No public-state field, recovery counter,
manifest-key alias, tool-provenance field, credential, token, operator-selected
stage, or new scientific input is added.

All execution-root, authority, process, output, preparation, verification,
Windows claim/receipt, arm, lifecycle, finalization-bundle, emergency-bundle,
result-owner, and result-work paths remain unchanged. They may be reused only
because their incorporated absence gate and the observed O8v2 stop state prove
that O8v2 never created them. Any existing, unsafe, nonempty, or ambiguous path
stops O8v3 before bootstrap; it is never cleaned, adopted, or repaired.

The treatment ID, diagnostic ID, comparison semantics, all 140 scientific rows,
all twelve public scenes, every transform, role, control, selector, datum,
tolerance, reason, fallback, scientific resource counter, analysis rule,
dependency, scientific function body, payload field, payload limit, and the
2,100/2,400/2,700-second scientific limits remain exactly as incorporated. A and
B remain deterministic observations of the same unchanged treatment.

The deterministic v7-to-v8 scientific-source transformation and inverse audit
remain unchanged except for declared administrative substitutions and the one
pre-science runner key correction. Reversing those declared changes must still
recover the same frozen v7 audit module exactly. No scientific code, data, row,
scene, transform, selector, control, or resource semantic may change.

All five authorization Booleans remain false. This document authorizes no
lockbox, sealed evaluation, gameplay, leaderboard submission, development
matrix, model change, positive mechanism claim, or third scientific start.

The following administrative values change prospectively:

    preregistration tag:       prereg-action-qbc-v8-open-bounded-remote-verification-v6
    preregistration commit:    P8v6_COMMIT
    preregistration document:  docs/experiment_amendment_2026-08-18_action_qbc_v8_open_bounded_remote_verification_v6_runner_manifest_key_recovery.md
    document Git blob:         P8v6_DOCUMENT_GIT_BLOB_SHA1
    document SHA-256:          P8v6_DOCUMENT_SHA256
    document byte count:       P8v6_DOCUMENT_BYTE_COUNT
    open-freeze branch:        action-qbc-v8-prereg
    open-freeze tag:           action-qbc-v8-open-diagnostic-freeze-v3
    result branch:             action-qbc-v8-open-diagnostic-result
    result tag:                action-qbc-v8-open-diagnostic-result-v3

Registration content/file hashes, preregistration-tree manifest, O8v3
added-file manifest, source identities, and argv hashes are regenerated from the
replacement bytes. Changed identities are not schema changes.

## 8. Exact P8v6 reset and O8v3 tree boundary

P8v6 deletes all and only these fourteen O8v2 non-registration paths:

    docs/action_qbc_v8_open_diagnostic_runbook.md
    scripts/build_action_qbc_v8_open_registration.py
    scripts/execute_action_qbc_v8_open_lifecycle.py
    scripts/finalize_action_qbc_v8_open_diagnostic.py
    scripts/prepare_action_qbc_v8_open.py
    scripts/reconstruct_action_qbc_v8_open_registration.py
    scripts/run_action_qbc_v8_open_diagnostic.py
    scripts/supervise_action_qbc_v8_remote_tag.py
    scripts/validate_action_qbc_v8_open_payload.py
    scripts/verify_action_qbc_v8_remote_tag.py
    src/arc3_voi/action_qbc_v8_audit.py
    tests/test_action_qbc_v8_audit.py
    tests/test_action_qbc_v8_lifecycle.py
    tests/test_action_qbc_v8_registration.py

P8v6 also deletes the O8v2 registration:

    artifacts/action_qbc_v8_open_registration.json

Together with the sole P8v6 document addition, those deletions are P8v6's
complete direct diff from O8v2. The builder path is deliberately re-added
byte-identically in O8v3; a no-op edit is forbidden. The other authored paths
may differ only for the frozen runner correction, lineage/ref/public-state/tool
identity substitutions, documentation, and necessary regression tests.

After P8v6 is committed, tagged, and successfully published, O8v3 re-adds all
and only the fourteen non-registration paths above. They must be the only staged
paths, each stage 0 and mode `100644`, before the producer is invoked exactly
once with:

    ["uv","run","--frozen","--extra","dev","python3","-I","-B","scripts/build_action_qbc_v8_open_registration.py","--repository-root",".","--preregistration-tag","prereg-action-qbc-v8-open-bounded-remote-verification-v6","--output","artifacts/action_qbc_v8_open_registration.json"]

It creates the sole fifteenth addition:

    artifacts/action_qbc_v8_open_registration.json

The registration must be a no-follow regular worktree file owned by the
registered Linux user, mode `0600`, with canonical JSON and no final LF. Its
staged Git mode is `100644`. Its reconstruction must be byte-identical.

The incorporated exact index, manifest, registration, reconstruction, pytest,
Ruff, strict-mypy, source-transformation, raw-byte, tool, environment,
no-science, and post-commit gates remain binding. Each producer, reconstructor,
and frozen gate invocation retains its one-shot no-retry meaning. A failed or
partial construction is not O8v3 and requires another preregistration.

Only after every gate passes may all and only the fifteen additions be committed
as P8v6's direct child and tagged freeze-v3.

## 9. Authority and attempt command plans

The canonical authority plan contains exactly 46 argv rows, in this order:

1. the incorporated initial config query;
2. the active freeze-v3 tag-type, tag-resolution, and HEAD rows;
3. the four incorporated rows for each of P8v1, P8v2, P8v3, and P8v4:
   tag type, tag resolution, sole-parent relation, and registered diff;
4. four historical O8v1 rows: freeze-v1 tag type, tag resolution, sole parent
   P8v4, and exact P8v4-to-O8v1 A15 diff;
5. four historical P8v5 rows: v5 tag type, tag resolution, sole parent O8v1,
   and exact O8v1-to-P8v5 D15+A1 diff;
6. four historical O8v2 rows: freeze-v2 tag type, tag resolution, sole parent
   P8v5, and exact P8v5-to-O8v2 A15 diff;
7. four P8v6 rows: v6 tag type, tag resolution, sole parent O8v2, and exact
   O8v2-to-P8v6 D15+A1 diff;
8. the active P8v6 document `ls-tree` and `cat-file` rows;
9. the O8v3 sole-parent P8v6 row and exact P8v6-to-O8v3 A15 diff row; and
10. the incorporated post config, HEAD, tree, batch-object, index, and status
    rows.

The P8v5-to-P8v6 net-A1 relation is independently validated even where it is
not a separate authority-command row. All producer and consumer implementations
must generate the same byte-identical 46-row order.

The execution-phase remote-attempt command plan remains exactly 54 rows for
each permitted attempt. Its commands, order, maximum of three internal attempts,
timeouts, delay, output caps, cleanup, and evidence semantics are unchanged.
Those internal attempts are part of the later one-shot claimed lifecycle and
are not retries of the sole pre-bootstrap O8v3 anonymous verification.

## 10. Existing PUBLIC state and preregistration publication

The repository is already PUBLIC as the recorded result of O8v2's sole
successful visibility transition. PUBLIC is an inherited precondition, not a
new operator action. This recovery authorizes no `gh repo edit`, browser
visibility mutation, API visibility mutation or query, PUBLIC-to-PRIVATE
transition, second PRIVATE-to-PUBLIC transition, or visibility rehearsal.

The existing branch has exactly two additional registered non-force
fast-forwards after O8v2:

1. O8v2 to its direct child P8v6, atomically with the v6 preregistration tag;
2. P8v6 to its direct child O8v3, atomically with the freeze-v3 tag.

After P8v6 passes its local commit audit, publish it to the already-PUBLIC
repository with exactly one non-force atomic Windows Git invocation:

    ["C:\\Users\\User\\anaconda3\\Library\\bin\\git.exe","--no-replace-objects","--no-optional-locks","-C","D:\\kaggle competitions\\arc3-crosslevel-voi","push","--atomic","origin","refs/heads/action-qbc-v8-prereg:refs/heads/action-qbc-v8-prereg","refs/tags/prereg-action-qbc-v8-open-bounded-remote-verification-v6:refs/tags/prereg-action-qbc-v8-open-bounded-remote-verification-v6"]

Failure, partial or non-atomic behavior, uncertainty, or unexpected ref movement
stops before O8v3 construction. The invocation is never retried under this
preregistration. It is publication, not an anonymous connectivity rehearsal.

After O8v3 and every post-commit audit pass, publish the branch and freeze-v3
tag to the already-PUBLIC repository with exactly one non-force atomic Windows
Git invocation:

    ["C:\\Users\\User\\anaconda3\\Library\\bin\\git.exe","--no-replace-objects","--no-optional-locks","-C","D:\\kaggle competitions\\arc3-crosslevel-voi","push","--atomic","origin","refs/heads/action-qbc-v8-prereg:refs/heads/action-qbc-v8-prereg","refs/tags/action-qbc-v8-open-diagnostic-freeze-v3:refs/tags/action-qbc-v8-open-diagnostic-freeze-v3"]

Failure, partial or non-atomic behavior, uncertainty, or unexpected ref movement
stops before anonymous verification and bootstrap. The invocation is never
retried. No ref is forced, deleted, rewound, repointed, or reused.

No remote query, browser refresh used as a protocol check, API query,
authenticated ref query, SSH query, anonymous ref query, connectivity probe, or
visibility operation may occur between the P8v6 push and O8v3 push or between
the O8v3 push and section 11's sole query. Necessary transport interaction by
the two registered pushes is not a separate query.

## 11. Sole O8v3 anonymous verification

After successful O8v3 atomic publication, the next protocol network action is
exactly one credential-disabled read from neutral WSL cwd
`/mnt/d/kaggle competitions` with an empty environment:

    ["C:\\Windows\\System32\\wsl.exe","-d","Ubuntu","--cd","D:\\kaggle competitions","--","/usr/bin/env","-i","GIT_CONFIG_COUNT=0","GIT_CONFIG_GLOBAL=/dev/null","GIT_CONFIG_NOSYSTEM=1","GIT_NO_REPLACE_OBJECTS=1","GIT_TERMINAL_PROMPT=0","HOME=/home/bansarinejad","LANG=C","LC_ALL=C","PATH=/usr/local/bin:/usr/bin:/bin","XDG_CONFIG_HOME=/nonexistent","/usr/bin/git","--no-replace-objects","-c","credential.interactive=never","-c","core.askPass=","-c","credential.helper=","ls-remote","--refs","https://github.com/bansarinejad/arc3-crosslevel-voi.git","refs/heads/action-qbc-v8-prereg","refs/tags/action-qbc-v8-open-diagnostic-freeze-v3"]

It is invoked exactly once. Stdout is parsed as records, not presentation order.
Success requires exactly these two records, each exactly once, both at the
recorded lowercase 40-hex O8v3 commit:

    refs/heads/action-qbc-v8-prereg
    refs/tags/action-qbc-v8-open-diagnostic-freeze-v3

Stderr must be empty. A peeled, duplicate, missing, additional, malformed,
uppercase, wrong-hash, credential-assisted, or authenticated result is rejected.

Any spawn failure, nonzero exit, timeout, empty or unexpected output, nonempty
stderr, wrong remote state, visibility uncertainty, or parsing ambiguity stops
O8v3 before bootstrap. The check is consumed whether it succeeds or fails and
is never retried. There is no credential, alternate URL, helper, browser, API,
SSH, or later pre-bootstrap check. A later attempt requires a new prospective
preregistration and fresh immutable refs.

Only that exact successful two-ref result authorizes the incorporated read-only
host preflight and one-shot authority bootstrap for O8v3. O8v1's failed check
and O8v2's successful check cannot authorize O8v3.

## 12. Downstream execution and result publication

After successful O8v3 anonymous verification, the incorporated bootstrap,
preparation, independent preparation verification, Windows supervisor, Linux
lifecycle, finalizer, publisher, transfer, and result-verification protocol
applies unchanged except for P8v6, O8v3, R8v3, the corrected Linux Git SHA, and
their derived administrative identities.

The authority bootstrap clones:

    action-qbc-v8-open-diagnostic-freeze-v3

and checks out the recorded O8v3 commit. It never executes freeze-v1 or
freeze-v2. The ten incorporated bootstrap argv arrays, stdout requirements,
filesystem side effects, detached open-freeze reconstruction check,
preparation, preparation-verification receipts, supervisor, lifecycle,
absence/no-cleanup rules, and no-retry rules remain otherwise unchanged.

If R8v3 is validly created, its local and remote identities use only:

    refs/heads/action-qbc-v8-open-diagnostic-result
    refs/tags/action-qbc-v8-open-diagnostic-result-v3

The incorporated immutable-result transaction, publication-only recovery, local
transfer, bounded result pushes, and final result two-ref verification remain
unchanged. Neither result-v1 nor result-v2 may be created or accepted.

PUBLIC visibility must continue without mutation through anonymous verification,
bootstrap, preparation, execution, R8v3 transfer, result publication, and final
result-ref verification. A visibility change is terminal administrative failure
and authorizes no retry.

## 13. Required implementation and regression tests

Before O8v3, all incorporated offline tests and exact frozen gates remain
mandatory. Additional tests must prove:

- O8v2 is P8v5's exact-fifteen-addition direct child at its immutable commit,
  tree, and freeze-v2 tag;
- P8v6 is O8v2's exact D15+A1 direct child, its tree equals P8v5 plus only the
  P8v6 document, and P8v5-to-P8v6 is net A1;
- O8v3 is P8v6's exact-fifteen-addition direct child and freeze-v3 is a
  lightweight tag at only O8v3;
- every historical preregistration and freeze tag remains immutable at its exact
  historical commit, and every force, deletion, rewind, replacement, or ref
  reuse fails;
- the complete O8v1 and O8v2 disclosures cannot be represented as unconsumed,
  retriable, scientifically executed, or result-producing states;
- no O8v2 bootstrap/root/claim/preparation/lifecycle/science/result state exists;
- the real canonical registration source-manifest shape passes the runner's
  reconstructed-execution validator using `open_freeze_added_files`;
- `added_in_open_freeze` is absent from runtime consumers and is rejected rather
  than treated as a compatibility alias;
- a generated registration, not only a hand-built fixture, exercises the
  runner validation path that exposed the defect;
- the registration retains its v1 schema, exact 70-key execution contract, and
  all incorporated scientific, receipt, claim, path, timing, and resource
  identities except declared administrative substitutions;
- the only O8v2-to-O8v3 nonadministrative executable change is the exact runner
  key correction, and scientific-source reversal remains identical;
- `/usr/bin/git` uses the corrected path/version/SHA identity, while all six
  other Linux and all Windows tool identities remain unchanged;
- producer, reconstructor, preparation, lifecycle, finalizer, runner, and tests
  agree on the byte-identical 46-row authority plan and 54-row attempt plan;
- the branch permits only O8v2 -> P8v6 -> O8v3 fast-forwards and publication
  argv arrays contain only their paired immutable tags;
- already-PUBLIC state permits no visibility invocation or query, and any
  visibility mutation or uncertainty forbids O8v3 anonymous verification and
  bootstrap;
- the exact anonymous argv accepts only the branch and freeze-v3 once at O8v3
  with empty stderr, and any failure consumes the check without retry;
- bootstrap and downstream paths accept freeze-v3 and result-v3 while rejecting
  freeze-v1, freeze-v2, result-v1, and result-v2 without changing science; and
- tests remain offline, create no real execution root or claim, mutate no
  visibility or remote ref, import no scientific module on pre-start failure,
  and write no real result path.

The registration builder runs only after the fourteen authored additions are
staged. Registration reconstruction must be byte-identical. Full frozen pytest,
Ruff, and strict-mypy gates run once, in order, in the incorporated explicit
Ubuntu environment. Any failed, timed-out, ambiguous, or repeated one-shot gate
requires another preregistration.

## 14. Claims that remain prohibited

This recovery is a minimal administrative correction to a pre-science
source-manifest lookup and one observed tool identity. It is not evidence about
the Action-QBC mechanism, model quality, scientific rows, public scenes, unseen
generalization, causality, calibrated Bayesian inference, runtime readiness,
security, or Kaggle performance.

O8v2's successful anonymous publication check proves only that its two requested
refs were publicly readable at that observation. It does not validate O8v2's
unexecuted runner. O8v3's prospective check is likewise a bounded consistency
control, not cryptographic attestation, tamper-proofing, proof against a
malicious same-authority actor, or proof of historical execution.

No failed or incomplete replacement may be tuned away, retried, or described as
scientific evidence. It remains publishable only with its exact administrative
or scientific meaning.
