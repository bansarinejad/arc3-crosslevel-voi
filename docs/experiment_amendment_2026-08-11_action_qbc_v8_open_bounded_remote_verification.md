# Preregistered action-QBC v8 bounded-verification replication amendment

Date frozen: 11 August 2026 (Australia/Sydney)

Status: protocol written after the canonical v7 administrative terminal and the fully
disclosed connectivity and line-ending audits below, but before any v8 source, registration,
payload, scientific result, remote-verification claim or receipt, execution root, or
scientific process exists.

## 1. Purpose, identity, and non-repair boundary

This amendment defines one new open-only treatment:

- treatment: `action-qbc-v8-open-failure-decomposition-bounded-verification-v1`;
- diagnostic system: `crosslevel-voi-open-diagnostic-v8`;
- comparison semantics:
  `action-qbc-v8-v7-mathematics-identity-replication-v1`;
- registration: `action-qbc-v8-open-registration-v1`;
- scientific payload: `action-qbc-v8-open-diagnostic-payload-v1`;
- preparation receipt: `action-qbc-v8-preparation-receipt-v1`;
- remote invocation claim: `action-qbc-v8-remote-tag-verification-claim-v1`;
- remote verifier-start claim: `action-qbc-v8-remote-tag-verifier-start-claim-v1`;
- remote receipt: `action-qbc-v8-remote-tag-verification-receipt-v1`;
- remote supervisor receipt:
  `action-qbc-v8-remote-tag-verification-supervisor-receipt-v1`;
- arm receipt: `action-qbc-v8-arm-receipt-v1`;
- scientific start claim: `action-qbc-v8-scientific-start-claim-v1`;
- payload-validator claim: `action-qbc-v8-payload-validator-claim-v1`;
- payload-validation receipt: `action-qbc-v8-payload-validation-receipt-v1`;
- lifecycle-driver claim: `action-qbc-v8-lifecycle-driver-claim-v1`;
- lifecycle ledger: `action-qbc-v8-lifecycle-ledger-v1`;
- finalization bundle: `action-qbc-v8-finalization-bundle-v1`;
- emergency result bundle: `action-qbc-v8-emergency-result-bundle-v1`;
- result-Git owner claim: `action-qbc-v8-result-git-owner-claim-v1`;
- final receipt: `action-qbc-v8-open-diagnostic-receipt-v1`;
- administrative terminal: `action-qbc-v8-open-diagnostic-administrative-terminal-v1`;
- runtime identity: JSON `null`.

V8 is not a repair, continuation, completion, or rerun of the v7 lifecycle or result. V7
remains permanently terminal with zero scientific starts. V8 is a newly preregistered
replication that uses the frozen v7 mathematics under new Git boundaries, registration,
execution paths, process starts, payload identity, receipts, and result. Its v8 audit module
is the deterministic identity-only source transformation in section 4; every scientific
function body remains byte-identical after reversing that declared transformation.

V8 is adaptive public-fixture engineering. It has no lockbox, sealed phase, runtime arm,
gameplay arm, leaderboard authorization, development-matrix authorization, or positive
mechanism claim. Every authorization Boolean remains false:

```text
lockbox_generation_authorized
sealed_execution_authorized
runtime_admission_authorized
runtime_v8_enabled
final_admission_claimed
```

No v7 file, tag, commit, artifact, result path, execution root, or receipt may be modified,
replaced, deleted, backfilled, or adopted by v8.

## 2. Frozen ancestry and complete pre-preregistration disclosure

### 2.1 Immutable anchors

V8 starts from the canonical v7 result commit:

- v7 result commit `R7`:
  `6f918e098a9ea97cadbb377027a8eb5caeb9589b`;
- v7 result tag: `action-qbc-v7-open-diagnostic-result-v1`;
- v7 open-freeze commit `O7`:
  `851fb6dadc851d17ba9540165f48570ee4203ded`;
- v7 open-freeze tag: `action-qbc-v7-open-diagnostic-freeze-v1`;
- v7 preregistration commit:
  `f4a267757a7abbd72bc1aeb86e98811c521bf574`;
- v7 registration content SHA-256:
  `b09f9ee3b778222afd474645e64512ddc5abc3b6b326a2af9619ee016452a825`;
- v7 registration file SHA-256:
  `69520f0aa1eeb8ee38e744669a66e443c3e0637e4448200331f9ae6099ae499f`;
- v7 administrative-terminal SHA-256:
  `90826498333079cfe7640c21b618fc03f0ee32e53ea5e80ba1b8b72f542792ba`.

The immutable scientific modules are:

| Path | O7 Git blob | SHA-256 |
|---|---|---|
| `src/arc3_voi/action_qbc_v7_audit.py` | `97adf13b9fcb753565a0197ece00ebef13312d5d` | `559db2774e213abd5bec9dd073c6bfae7ccd5fdefedb7dbecbf0f96499cd81cd` |
| `src/arc3_voi/action_qbc_v7_reference.py` | `2d019b3d28524c75fd1657048ca1b67c145f3b97` | `34b24f96c5de4cad1026aa45ee388cf6dac1ee585ba42474a0a15ae216e46455` |

The v6 negative remains the latest scientific evidence. V7 produced no scientific
observation, so v8 must not describe v7 as scientifically positive, negative, mixed, or
completed.

### 2.2 Disclosed administrative observations

Before this amendment:

1. The v7 implementation freeze passed 121 tests, Ruff, strict mypy, and byte-identical
   registration reconstruction. Those checks did not start the full scientific evaluator.
2. The registered v7 WSL `git ls-remote` step emitted no stdout and was terminated by its
   host supervisor after approximately 64 seconds. The one-shot v7 finalizer therefore
   published `tag_verification_failed`; A and B were never started.
3. Windows GitHub access subsequently and repeatedly resolved the v7 branch and lightweight
   tags correctly. The active tools before P8 were Windows Python 3.12.3 from
   `C:\Users\User\anaconda3\python.exe` and Git 2.45.2 from
   `C:\Users\User\anaconda3\Library\bin\git.exe`.
4. One additional pre-P8 WSL connectivity rehearsal queried only the already public v7
   result tag with credential prompts disabled and a 120-second GNU `timeout`. It emitted no
   stdout and terminated after approximately 121 seconds. No scene, compiler, planner,
   selector, controller, scientific module, or output path was accessed.
5. Four tracked JSON files in the original Windows checkout were Git-clean after filtering
   but had raw CRLF or mixed worktree bytes while their committed blobs were LF:
   `artifacts/model_gate_live8.json`,
   `artifacts/model_gate_live8_pre_grounding.json`,
   `artifacts/prompt_grounding_bp35_seed11.json`, and
   `artifacts/public_games.snapshot.json`. Replacing CRLF with LF reproduced every committed
   byte count and SHA-256. The Git blobs and v7 registration were correct; the v7 finalizer
   incorrectly treated raw Windows materialization as committed identity and then discarded
   otherwise validated identities.

These observations fix only administrative design. No v8 outcome has been observed, no v8
extension scene has been evaluated, and no result-dependent choice is permitted after P8.

## 3. Immutable scientific contract

V8 retains the complete v7 scientific contract without improvement or reinterpretation:

- the same twelve public scenes, 140 rows, transforms, roles, controls, reasons, and
  aggregates in the same order;
- the same compiler, grounding, worker, planner, controller, v4, raw-selector,
  fixed-selector, compound-selector, and finalization functions;
- every tolerance, fixed quantum, dense-rank rule, tied-set rule, canonical tie-break,
  exception classification, and fallback precedence;
- CPython 3.12.13, uv 0.11.28, the exact `uv.lock`, NumPy 2.5.1, and PyYAML 6.0.3;
- the 2,100-second scientific compute deadline, 2,400-second runner wall limit, and
  2,700-second shell hard timeout;
- the exact 31-field resource counter vector.

The per-process counter vector is ordered exactly as in the frozen v7 registration. Its
nonzero values are:

```text
public_scene_generations                  12
candidate_builder_calls                   48
compiler_calls                            60
compiled_programs                        240
grounding_evaluations                    240
hypothesis_pool_constructions             60
persistent_worker_starts                 240
transient_worker_starts                  240
total_worker_starts                      480
planner_calls                             60
completed_planning_snapshots              60
controller_calls                          96
controller_snapshot_replays               96
v4_counterfactual_calls                   12
raw_selector_scene_order_calls           216
raw_selector_control_calls                19
fixed_selector_scene_order_calls         120
fixed_selector_control_calls              19
isolated_raw_selector_calls               96
isolated_fixed_selector_calls             96
pure_selector_calls                      566
```

`registered_scene_file_reads`, `model_calls`, `generated_tokens`, `gpu_operations`,
`network_calls`, `environment_actions`, `reward_observations`, `rhae_observations`,
`lockbox_path_operations`, and `lockbox_bytes_read` are zero.

Any change to one of those scientific values, functions, dependencies, deadlines, or call
ownership invalidates v8 and requires v9.

## 4. Deterministic v8 audit fork, payload, and call automaton

### 4.1 Exact source transformation

`src/arc3_voi/action_qbc_v8_audit.py` is generated from the exact O7 bytes of
`src/arc3_voi/action_qbc_v7_audit.py` identified in section 2. Starting from those bytes,
the following ASCII replacements are applied globally in the displayed order and with the
displayed occurrence counts. No other byte may change.

| Count | Old bytes | New bytes |
|---:|---|---|
| 1 | `prereg-action-qbc-v7-open-failure-decomposition-v1` | `prereg-action-qbc-v8-open-bounded-remote-verification-v1` |
| 1 | `action-qbc-v7-open-failure-decomposition-v1` | `action-qbc-v8-open-failure-decomposition-bounded-verification-v1` |
| 1 | `crosslevel-voi-open-diagnostic-v7` | `crosslevel-voi-open-diagnostic-v8` |
| 1 | `action-qbc-v7-boundary-compound-selector-decomposition-v1` | `action-qbc-v8-v7-mathematics-identity-replication-v1` |
| 1 | `action-qbc-v7-open-registration-v1` | `action-qbc-v8-open-registration-v1` |
| 1 | `action-qbc-v7-open-diagnostic-payload-v1` | `action-qbc-v8-open-diagnostic-payload-v1` |
| 1 | `action-qbc-v7-open-diagnostic-freeze-v1` | `action-qbc-v8-open-diagnostic-freeze-v1` |
| 1 | `f4a267757a7abbd72bc1aeb86e98811c521bf574` | `<P8_COMMIT>` |
| 1 | `docs/experiment_amendment_2026-08-10_action_qbc_v7_open_failure_decomposition.md` | `docs/experiment_amendment_2026-08-11_action_qbc_v8_open_bounded_remote_verification.md` |
| 1 | `fcd284ce499983fcc953f54a9f833e1b6d80a822384768f75cb18948d627a1a7` | `<P8_DOCUMENT_SHA256>` |
| 4 | `artifacts/action_qbc_v7_open_registration.json` | `artifacts/action_qbc_v8_open_registration.json` |
| 1 | `runtime_v7_enabled` | `runtime_v8_enabled` |

`<P8_COMMIT>` is replaced by the lowercase 40-hex commit created from this document alone;
`<P8_DOCUMENT_SHA256>` is replaced by SHA-256 of this document's committed blob bytes.
Those are literal one-time substitutions after P8 exists, not tunable values. The v8 module
continues to import the unchanged
`src/arc3_voi/action_qbc_v7_reference.py`. A byte-level reverse-transformation test, AST
comparison, function inventory comparison, and all frozen v7 audit fixtures must pass. The
new module is a truthful v8 identity fork, not a wrapper around a mismatched v7 payload.

### 4.2 Exact payload schema

The canonical v8 payload contains exactly these nineteen top-level keys:

```text
schema_version
treatment_id
diagnostic_system_id
comparison_semantics_id
runtime_id
preregistration_identity
v6_negative_identity
registration_identity
execution_identity
resource_counters
grid_evidence
expected_exterior_support
rows
aggregates
diagnostic_complete
scientific_capability_passes
authorization
terminal_fallback_stage
candidate_payload_size_bytes
```

`preregistration_identity` has exactly `commit_sha`, `tag`, `document_path`,
`document_git_blob_sha1`, and `document_sha256`. `v6_negative_identity` has exactly
`result_commit_sha`, `result_json_path`, `result_json_sha256`, `failure_vector_sha256`, and
`result_document_sha256`. `registration_identity` has exactly `schema_version`, `path`,
`content_sha256`, and `file_sha256`. `execution_identity` has exactly
`open_freeze_commit_sha`, `open_freeze_tag`, `source_manifest_sha256`, `python_version`,
`python_implementation`, `platform_system`, `platform_machine`, `uv_version`,
`uv_lock_sha256`, and `canonical_command_sha256`.

The remaining scientific objects, row schemas, evidence layers, reason order, aggregate
keys, nullability, numeric types, and list ordering are canonical-byte identical to the v7
registration contracts. Only the fixed identities and `runtime_v8_enabled` authorization
key change through section 4.1. Process label, concrete clone root, concrete output path,
attempt number, timing, and receipt data are forbidden from the payload; they exist only in
the final receipt or terminal. This makes A and B byte equality possible.

The payload cap is exactly 67,108,864 canonical v8 payload bytes. A size equal to the cap
passes. A larger normal candidate is replaced by the exact 140-row
`payload_size_limit_exceeded` v8 fallback, whose `candidate_payload_size_bytes` is the
oversized canonical v8 candidate size. Outside that fallback the field is null. The fallback
itself must fit the same cap or the runner fails administratively without publishing.

### 4.3 Exact scientific call automaton

The frozen orchestration anchor is O7
`scripts/run_action_qbc_v7_open_diagnostic.py`, Git blob
`0b133eaad4169015e08533c6b78a0797f71a0825`, SHA-256
`c25b11532a9e22943c452d1c51f54c27b197345840821ed9c343a1b0e9e248ac`.
The v8 runner preserves its scientific automaton while adding only pre-producer v8 receipt,
path, and identity checks.

After all runner preconditions pass, `produce_scientific_candidate` is called exactly once
with the current process root, the validated v8 registration, and the single monotonic
compute deadline. It is called at most once per runner start and is never retried. If it
returns before the deadline, `finalize_scientific_payload` is called once and the returned
payload is validated. The exact O7 scientific try boundary is retained: it contains the
pre-producer deadline check, producer call, post-producer deadline check, finalizer call,
first payload validation, and canonical encoding. Any `GlobalFallbackRequired` escaping any
operation in that boundary builds one fallback with its registered stage and carried size;
any other escaping exception builds one `evaluator_internal_error` fallback. An oversized
encoded candidate builds one size fallback. The selected payload is validated before
encoding. Staged-byte validation immediately before exclusive publication is outside that
scientific fallback boundary; its failure is administrative and cannot trigger another
producer or scientific fallback. Precondition failure makes zero producer calls. No branch
permits a second producer call.

The v8 payload never writes a v7 path and cannot backfill the v7 result. V8 authorization is
false even if every scientific row completes or reconciles.

## 5. P8-to-O8 source boundary

P8 is a direct child of R7 and contains only this document. No other P8 path may differ.

O8 is a direct child of P8. No path present at P8 may be modified, renamed, or deleted. The
only paths added at O8 are exactly:

```text
artifacts/action_qbc_v8_open_registration.json
docs/action_qbc_v8_open_diagnostic_runbook.md
scripts/build_action_qbc_v8_open_registration.py
scripts/execute_action_qbc_v8_open_lifecycle.py
scripts/finalize_action_qbc_v8_open_diagnostic.py
scripts/prepare_action_qbc_v8_open.py
scripts/reconstruct_action_qbc_v8_open_registration.py
scripts/run_action_qbc_v8_open_diagnostic.py
scripts/supervise_action_qbc_v8_remote_tag.py
scripts/validate_action_qbc_v8_open_payload.py
scripts/verify_action_qbc_v8_remote_tag.py
src/arc3_voi/action_qbc_v8_audit.py
tests/test_action_qbc_v8_audit.py
tests/test_action_qbc_v8_lifecycle.py
tests/test_action_qbc_v8_registration.py
```

The registration artifact is generated only after the other fourteen additions are staged.
All manifests are derived from Git object or staged-index bytes, never unfiltered worktree
bytes. The independent standard-library reconstructor must reproduce the canonical compact
ASCII registration byte-for-byte without importing either scientific module.

The exact pre-O producer and reconstructor argv arrays are:

```json
[
  ["uv","run","--frozen","--extra","dev","python3","-I","-B","scripts/build_action_qbc_v8_open_registration.py","--repository-root",".","--preregistration-tag","prereg-action-qbc-v8-open-bounded-remote-verification-v1","--output","artifacts/action_qbc_v8_open_registration.json"],
  ["uv","run","--frozen","--extra","dev","python3","-I","-B","scripts/reconstruct_action_qbc_v8_open_registration.py","--repository-root",".","--registration","artifacts/action_qbc_v8_open_registration.json"]
]
```

The post-O Linux identity audit uses exactly:

```json
["/usr/bin/python3","-I","-B","scripts/reconstruct_action_qbc_v8_open_registration.py","--repository-root",".","--registration","artifacts/action_qbc_v8_open_registration.json","--verify-open-freeze"]
```

The registration contains exactly these nineteen top-level keys:

```text
schema_version
status
treatment_id
diagnostic_system_id
comparison_semantics_id
runtime_id
preregistration
v6_negative
platform
dependencies
source_manifest
scene_inventory
row_inventory
transform_contracts
scientific_contract
resource_contract
execution_contract
authorization
content_sha256
```

`content_sha256` is SHA-256 of canonical sorted-key compact ASCII JSON for the other
eighteen keys. The complete file is the same encoding with `content_sha256` inserted and no
final line feed. The fixed identity values are section 1, status is
`registered_zero_result`, runtime is null, and authorization is the exact five-key false
object in section 1.

`preregistration` has exactly `commit_sha`, `tag`, `document_path`,
`document_git_blob_sha1`, and `document_sha256`. `v6_negative` has the five keys listed for
`v6_negative_identity` in section 4. `platform` has exactly `python_version`,
`python_implementation`, `platform_system`, `platform_machine`, and `uv_version`.
`dependencies` is canonical-byte equal to the v7 registration dependency array.
`source_manifest` has exactly `preregistration_tree`, `open_freeze_added_files`, and
`manifest_sha256`. `scene_inventory`, `row_inventory`, `transform_contracts`, and
`resource_contract` are canonical-byte equal to the v7 registration values.

`scientific_contract` has every exact v7 scientific-contract key and value plus one key,
`audit_source_transformation`, whose value is the ordered replacement table in section 4,
the two frozen input-module identities, and the generated v8 audit Git blob/SHA-256/byte
count. `execution_contract` has exactly:

```text
administrative_stage_order
argv_hashes
arm_argv
arm_receipt_path
arm_timeout_seconds
authority_root
bootstrap_steps
compute_deadline_seconds
environment_build_argv
emergency_bundle_path
execution_root
finalization_bundle_path
linux_host_launcher
linux_platform
linux_tool_identities
lifecycle_driver_argv
lifecycle_driver_claim_path
driver_deadline_seconds
lifecycle_ledger_path
local_git_timeout_seconds
finalizer_argv_template
finalizer_cwd
finalizer_timeout_seconds
hard_timeout_seconds
payload_validator_argv_template
payload_validator_timeout_seconds
preflight_argvs
post_preparation_validation_argv
preparation_argv
preparation_receipt_path
process_a_output
process_a_root
process_a_start_claim
process_a_validation_receipt
process_a_validator_claim
process_b_output
process_b_root
process_b_start_claim
process_b_validation_receipt
process_b_validator_claim
process_labels
producer_argv
reconstructor_argv
registered_start_count
remote_claim_linux_path
remote_claim_windows_path
remote_policy
remote_receipt_linux_path
remote_receipt_windows_path
remote_supervisor_argv
remote_supervisor_receipt_linux_path
remote_supervisor_receipt_windows_path
remote_verifier_claim_linux_path
remote_verifier_claim_windows_path
remote_verifier_argv
result_document_contract
result_git_environment
result_git_max_attempts
result_git_owner_path
result_git_work_root
result_publisher_argv
result_ref_transaction
scientific_argv_template
test_argvs
third_start_allowed
wall_time_seconds
```

Every command collection is an ordered JSON string array or ordered list of such arrays.
`argv_hashes` maps each named command/template to SHA-256 of its canonical JSON value.
`administrative_stage_order` has exactly `underlying_order` (the section 9 precedence) and
`disposition_overrides` (exactly
`["receipt_finalization_failed","finalizer_process_failed"]`).
`remote_policy` has the exact schema and values in section 7. The registration binds:

- R7, P8, O7, the v7 registration, the two frozen modules, `uv.lock`, configuration, and
  every transitive local dependency by Git blob, SHA-256, and byte count;
- all P8 tree entries and fourteen pre-registration O8 additions;
- the v7 scene, row, transform, scientific, resource, reason, and authorization contracts;
- the exact audit transformation and flat v8 payload contract;
- all platform identities, paths, commands, command hashes, deadlines, caps, and stage
  precedence;
- the preparation, remote claim/receipt, arm, process, lifecycle, final-receipt, terminal,
  normal-bundle, and emergency-bundle schemas;
- the Linux raw-byte audit, A/B validation, finalizer, and Git-only result-publication
  contracts.

## 6. Git-object and Linux materialization contract

Committed Git objects are authoritative. For each registered source path, the builder and
reconstructor enumerate stage-zero entries with `git ls-files --stage -z`, obtain staged or
tree bytes with `git cat-file --batch`, recompute the Git blob SHA-1, SHA-256, and byte
count, and compare the exact path/mode inventory. `Path.read_bytes`, unfiltered worktree
hashing, and filter-clean status are never manifest identity. The registration is separately
self-bound by content SHA-256, file SHA-256, and its O8 Git blob.

Every Git subprocess in the Linux preparation, reconstruction, audit, lifecycle, finalizer,
and publication-transfer code uses the literal executable `/usr/bin/git`; prose that names a
`git` subcommand is shorthand for that fixed executable and never PATH lookup.

Every Windows-to-Linux lifecycle invocation is made from the same interactive Windows user
context with this exact outer launcher form:

```json
["C:\\Windows\\System32\\wsl.exe","-d","Ubuntu","--cd","<REGISTERED_LINUX_CWD>","--","<INNER_ARGV...>"]
```

Bare `wsl.exe`, an omitted `-d Ubuntu`, an implicit default distribution, and any other
outer launcher are forbidden. The host preflight must report Ubuntu as WSL version 2 and
must match all frozen identities before bootstrap. The registered host tool is
`C:\Windows\System32\wsl.exe`, product version `10.0.26100.8737`, SHA-256
`7e9f5cee6d641481e5a942f0e08563bae9c17ee55f0aad888f9aa0be9a5d4757`. The registered
Linux platform is Ubuntu 24.04.1 LTS (`noble`), kernel
`5.15.167.4-microsoft-standard-WSL2`, machine `x86_64`; its tools are CPython 3.12.3 at
`/usr/bin/python3`, uv 0.11.28 at `/usr/local/bin/uv`, Git 2.43.0 at `/usr/bin/git`, and
GNU coreutils `timeout` 9.4 at `/usr/bin/timeout`. The default distribution observed before
P8 was `docker-desktop`, so exact distro selection is a fail-closed identity condition, not
a convenience. The host preflight is read-only and imports no project module.

`linux_tool_identities` is a path-sorted array whose objects have exactly `path`, `version`,
and `sha256`:

| Path | Version | SHA-256 |
|---|---|---|
| `/usr/bin/env` | GNU coreutils 9.4 | `1490a663e7312c4347987b2e12d7d73950ed1e9a322449daf8e4836660396e31` |
| `/usr/bin/git` | 2.43.0 | `953577d782b6a4dada93cdb924a1261266c7b98aae6676e4ddeeddfc9a848e8e` |
| `/usr/bin/install` | GNU coreutils 9.4 | `b4663b43190ea551f682cfac9500f3f4f6e94890d8ce8822bb81a819f15dab00` |
| `/usr/bin/python3` | CPython 3.12.3 | `1643dacd9feaedc58f3cc581e4d22577dfe25c09b10282936186ccf0f2e61118` |
| `/usr/bin/test` | GNU coreutils 9.4 | `52b0ca5cef7e104ad5e0a8a29bd1522c205cc8404e46e153e5afc54605857c4d` |
| `/usr/bin/timeout` | GNU coreutils 9.4 | `2ee918a5358c0388719e710134bc32cffb934f4bd2a8fb9beb86ef4d6ec8bd8a` |
| `/usr/local/bin/uv` | 0.11.28 | `1cb9cd0a1749debf6049d7d2bb933882cc52d81016326ee6d99a786d6c988b03` |

Before bootstrap, the read-only host preflight recomputes these raw executable hashes through
the exact Ubuntu launcher; a symlink is resolved to the executed target bytes. Bootstrap
starts only after they match, and each later registered program rechecks the identities
before spawning a child. Any mismatch leaves v8 unarmed.

Before remote verification, the operator prepares exactly three distinct Linux-native
clones under:

```text
/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/authority
/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/processes/process-a
/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/processes/process-b
```

The fixed local source URL is:

```text
file:///mnt/d/kaggle%20competitions/arc3-crosslevel-voi
```

From cwd `/var/tmp`, authority bootstrap executes these exact command templates in order:

```json
[
  ["/usr/bin/test","!","-e","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open"],
  ["/usr/bin/install","-d","-m","700","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open"],
  ["/usr/bin/install","-d","-m","700","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/authority"],
  ["/usr/bin/git","clone","--no-local","--no-checkout","--branch","action-qbc-v8-open-diagnostic-freeze-v1","--single-branch","file:///mnt/d/kaggle%20competitions/arc3-crosslevel-voi","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/authority"],
  ["/usr/bin/git","-C","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/authority","config","--local","core.autocrlf","false"],
  ["/usr/bin/git","-C","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/authority","config","--local","core.eol","lf"],
  ["/usr/bin/git","-C","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/authority","config","--local","core.safecrlf","true"],
  ["/usr/bin/git","-C","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/authority","checkout","--detach","<O8_COMMIT>"],
  ["/usr/bin/git","-C","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/authority","rev-parse","HEAD"]
]
```

The precreated empty authority destination fixes its mode before clone; Git must retain
mode 0700. The bootstrap sequence is invoked once. A nonzero exit, unexpected stdout, wrong HEAD, or
partial root leaves v8 unarmed; no source, remote, or scientific command follows and a new
preregistration is required. There is no bootstrap cleanup or retry.

The root and authority must be plain, owner-controlled mode-0700 directories with no
symlink ancestor. The tag must be lightweight (`git cat-file -t` returns `commit`) and all
HEAD results must equal O8. Before any other preparation, the authority runs the registered
standard-library reconstructor in O8 verification mode; it performs the raw audit below.

From the authority cwd, the sole preparation invocation is:

```json
["/usr/bin/python3","-I","-B","scripts/prepare_action_qbc_v8_open.py","prepare","--repository-root",".","--registration","artifacts/action_qbc_v8_open_registration.json","--execution-root","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open","--receipt","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/preparation-receipt.json"]
```

It makes at most two internal preparation attempts. Attempt `i` precreates one fresh
owner-controlled mode-0700 staging parent named `.prepare-attempt-i`, empty mode-0700
`process-a` and `process-b` destinations, and empty mode-0700 `process-a-output/open` and
`process-b-output/open` directories beneath it. It clones into the process destinations with
the same `clone --no-local --no-checkout --branch ... --single-branch` argv and source URL,
writes the same three clone-local Git settings, and checks out detached O8. An exclusive
owner marker is created in the staging parent, validated during failure cleanup, and removed
before success. Only after both clones, environments, and preflights pass does one same-device
atomic rename promote the entire staging parent to the previously absent `processes` path.
This makes both final clone roots visible in one transaction. On failure it may remove only
the staging parent whose device, inode, owner, mode, marker, and path it created and verified;
it records the failed attempt before the next. A second attempt is allowed only after the
first attempt's cleanup passes. It never removes or changes authority, a
final `processes` root, a source file, or a foreign path. If both attempts fail, it publishes
a canonical failure preparation
receipt, returns nonzero, and v8 remains unarmed: the remote supervisor and all scientific
commands are forbidden and no v8 result is claimed. The preparation program itself is not
invoked again.

For each clone, the raw audit enumerates the O8 tree, reads every blob through `git
cat-file`, recomputes Git SHA-1/SHA-256/length, and requires each checkout regular file to
equal its blob byte-for-byte. Symlinks, submodules, mode changes, missing paths, extra
tracked paths, dirty tracked files, and untracked files fail. The registration raw bytes
must equal its O8 blob. The audit occurs before environment creation.

Only after both raw audits pass, preparation executes in each process clone:

```json
["/usr/bin/env","UV_OFFLINE=1","/usr/local/bin/uv","sync","--python","3.12.13","--frozen","--no-dev","--offline"]
```

It then repeats the complete tracked-file raw audit. The only permitted non-Git filesystem
addition inside a process clone is the ignored plain `.venv` directory produced by that
exact uv command; `.git` is the sole administrative exclusion. No `.venv` is allowed in
authority. The preparation receipt contains exactly:

```text
schema_version
treatment_id
open_freeze_commit_sha
open_freeze_tag
registration_content_sha256
attempts
authority
process_a
process_b
commands_sha256
status
```

Each clone object has exactly `root`, `head_sha`, `tree_sha256`,
`raw_materialization_sha256`, `git_status_sha256`, `python_version`, `uv_version`,
`environment_inventory_sha256`, and `passes`. `authority` is always a complete clone object
and uses null only for its three environment fields. `process_a` and `process_b` are both
complete passing clone objects when status is `prepared`; both are null when status is
`failed`, because no staging parent was promoted. Status is exactly `prepared` or `failed`.
The environment inventory is the name-sorted exact list of installed distributions, each
with lowercase normalized name, version, and SHA-256 of the path-sorted file-name/size/hash
records; it must contain only the frozen project package, NumPy 2.5.1, and PyYAML 6.0.3 plus
packaging metadata installed by the frozen lock. No network-backed package operation is
permitted.
Each attempt has exactly `attempt_index`, `process_a_stage`, `process_b_stage`, `cleanup`,
`promotion`, and `passes`. A process stage is exactly `not_started`, `clone_failed`,
`raw_audit_failed`, `environment_failed`, `preflight_failed`, or `completed`. `cleanup` has
exactly `owned_paths`, `removed`, and `passes`; paths are sorted absolute strings, `removed`
is the sorted subset actually removed, and the Boolean is true only when no owned staging
path remains. Canonical hashes use the sorted-key compact ASCII JSON preimage of the named
records, and the receipt has no final line feed.

`promotion` has exactly `source_path`, `destination_path`, `source_device`, `source_inode`,
and `passes`. Paths are the fixed staging and final parent; device/inode are nonnegative
integers once the staging parent exists and otherwise null. Promotion passes only when the
verified source inode was atomically renamed, the source path is absent, and the destination
has that same device/inode with both complete clones and empty output parents. On a passing
promotion, cleanup records the owned staging path, an empty `removed` list, and passes
because that staging pathname no longer exists. On a failed promotion, cleanup must remove
the verified owned staging parent before it can pass. Attempt `passes` is true only for two
completed process stages, passing promotion, and passing cleanup.

The registered hash preimages are exact. `tree_sha256` hashes the canonical ordered array
of objects with exactly `mode`, `path`, and `git_blob_sha1`, sorted by UTF-8 path bytes.
`raw_materialization_sha256` hashes the same array extended with exactly `sha256` and
`size_bytes` for each raw checkout file. `git_status_sha256` hashes the exact stdout bytes
of `git status --porcelain=v1 -z --untracked-files=all` (the passing value is SHA-256 of
empty bytes). `environment_inventory_sha256` hashes the canonical distribution inventory
defined above. `commands_sha256` hashes the canonical ordered array of every fully
substituted argv actually attempted, including staging paths and attempt index, followed by
each integer exit code; a command not started is absent. All canonical arrays use compact
sorted-key ASCII JSON with no NaN or final line feed. Attempt records are present for every
started attempt in ascending index. On failed status, nullable unobserved command facts stay
null and no placeholder digest is invented.

After environment construction, each process preflight runs these exact argv arrays in
order:

```json
[
  ["/usr/bin/git","status","--porcelain=v1","-z","--untracked-files=all"],
  ["/usr/bin/git","rev-parse","HEAD"],
  [".venv/bin/python3","--version"],
  ["/usr/local/bin/uv","--version"],
  ["/usr/bin/python3","-I","-B","scripts/reconstruct_action_qbc_v8_open_registration.py","--repository-root",".","--registration","artifacts/action_qbc_v8_open_registration.json","--verify-open-freeze"]
]
```

Expected stdout is respectively empty, `<O8_COMMIT>\n`, `Python 3.12.13\n`,
`uv 0.11.28\n`, and the reconstructor's registered canonical verification record.

After the sole preparation process returns and before any supervisor command, authority
invokes this exact read-only independent gate once:

```json
["/usr/bin/python3","-I","-B","scripts/reconstruct_action_qbc_v8_open_registration.py","--repository-root",".","--registration","artifacts/action_qbc_v8_open_registration.json","--verify-preparation","--preparation-receipt","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/preparation-receipt.json"]
```

It revalidates the registration, canonical prepared-status receipt, command hashes, attempt
and promotion semantics, authority identity, both final clone/environment identities, empty
output parents, and exact process paths without importing a scientific module. It exits zero
and emits the registered canonical verification record only for a complete `prepared`
receipt. There is no retry; any other exit forbids the supervisor and requires a new
preregistration.

Preparation may be diagnosed before remote verification because no scientific module may be
imported and no scientific process, remote claim, or remote supervisor has started. The
supervisor is forbidden unless the one preparation invocation exits zero and its receipt is
independently accepted by the authority reconstructor.

The authoritative finalizer runs only from the Linux `authority` clone. It validates
registration identity, O8 tag/HEAD identity, and raw materialization as separate layers. A
later materialization failure must not erase an already validated registration or commit
identity. The original `/mnt/d` checkout is never used for authoritative finalization. The
authority clone remains a detached O8 worktree and supplies the Git object database in
which R8 is constructed through a temporary index. Result bytes never need to overwrite,
repair, clean, or even materialize in that worktree, so an authority-materialization failure
can itself remain intact as evidence while R8 is still published.

## 7. Bounded Windows remote verification and arm receipt

Required P8/O8 publication pushes and independent remote-ref checks occur before the
execution phase and are not scientific-lifecycle commands. Within the prepared O8 execution
phase, WSL performs no GitHub or HTTP request. The only registered online mechanism is one
invocation of the Windows supervisor from the original clean O8 checkout; its one verifier
child may make the bounded Git attempts below.

Before its exclusive lifecycle claim, the supervisor requires exactly the raw bytes of
`scripts/supervise_action_qbc_v8_remote_tag.py`,
`scripts/verify_action_qbc_v8_remote_tag.py`, and
`artifacts/action_qbc_v8_open_registration.json` to equal their O8 blobs and validates local
HEAD/tag identity. It does not require raw equality for any inherited Windows worktree path;
their committed-object identity was already established at O8. The canonical supervisor
argv is:

```json
["C:\\Users\\User\\anaconda3\\python.exe","-I","-B","scripts\\supervise_action_qbc_v8_remote_tag.py","--repository-root",".","--registration","artifacts\\action_qbc_v8_open_registration.json","--claim","D:\\kaggle competitions\\arc3-crosslevel-voi-action-qbc-v8-remote-verification-claim.json","--verifier-start-claim","D:\\kaggle competitions\\arc3-crosslevel-voi-action-qbc-v8-remote-verifier-start-claim.json","--remote-receipt","D:\\kaggle competitions\\arc3-crosslevel-voi-action-qbc-v8-remote-verification.json","--supervisor-receipt","D:\\kaggle competitions\\arc3-crosslevel-voi-action-qbc-v8-remote-verification-supervisor.json","--verifier-python","C:\\Users\\User\\anaconda3\\python.exe","--git-executable","C:\\Users\\User\\anaconda3\\Library\\bin\\git.exe","--taskkill-executable","C:\\Windows\\System32\\taskkill.exe","--verifier-child-deadline-seconds","440","--supervisor-deadline-seconds","480","--child-cleanup-timeout-seconds","30"]
```

Its cwd is exactly `D:\kaggle competitions\arc3-crosslevel-voi`. Fixed executable identities
are:

| Tool | Version | SHA-256 |
|---|---|---|
| `C:\Users\User\anaconda3\python.exe` | CPython 3.12.3 | `62c225fb9cdc41b139c7024581c233644f975ffc35314558c60ebefa6b88be01` |
| `C:\Users\User\anaconda3\Library\bin\git.exe` | 2.45.2.windows.1 | `5385ff9ae361ca41e7a31b335fc0d81f2de9c35fc62a165c5e34850d837b59cc` |
| `C:\Windows\System32\taskkill.exe` | file/product version 10.0.26100.8457 | `1249717315fc8f4d2df17d5db9da0444795fdb9fb83dfb1f763c3f39282244f7` |

Before starting the verifier child, the supervisor exclusively creates, flushes,
`os.fsync`s, reopens, hashes, and canonical-validates the lifecycle claim. It never replaces
an existing claim. The claim contains exactly:

```text
schema_version
treatment_id
open_freeze_commit_sha
open_freeze_tag
registration_content_sha256
supervisor_argv_sha256
supervisor_script_git_blob_sha1
supervisor_script_sha256
verifier_script_git_blob_sha1
verifier_script_sha256
```

Exclusive claim creation is the irreversible start of the v8 execution lifecycle.
Everything in section 6 is unarmed preparation; it cannot observe science or remote state
and is not a scientific or administrative result. After claim creation, the registered
control flow targets exactly one finalizer process-start attempt, normal-or-emergency bundle
selection, and R8 publication; the finalizer is never attempted twice. This is not an
OS-crash guarantee: an exogenous kill, host shutdown, hardware failure, or corruption of the
driver process itself before it emits a bundle can strand v8 after the claim. Such a case
permits no driver/science/finalizer retry, no v8 R8 claim, and no positive result; it requires
a separately preregistered later administrative disclosure. All protocol-level child hangs
and timeouts remain handled by the registered bounds and emergency path.

An existing lifecycle claim makes every later supervisor invocation fail before starting a
child. The supervisor starts exactly one verifier child in a new Windows process group with
this exact argv:

```json
["C:\\Users\\User\\anaconda3\\python.exe","-I","-B","scripts\\verify_action_qbc_v8_remote_tag.py","--repository-root",".","--registration","artifacts\\action_qbc_v8_open_registration.json","--claim","D:\\kaggle competitions\\arc3-crosslevel-voi-action-qbc-v8-remote-verification-claim.json","--verifier-start-claim","D:\\kaggle competitions\\arc3-crosslevel-voi-action-qbc-v8-remote-verifier-start-claim.json","--receipt","D:\\kaggle competitions\\arc3-crosslevel-voi-action-qbc-v8-remote-verification.json","--git-executable","C:\\Users\\User\\anaconda3\\Library\\bin\\git.exe","--taskkill-executable","C:\\Windows\\System32\\taskkill.exe","--max-attempts","3","--attempt-timeout-seconds","120","--retry-delay-seconds","15","--overall-deadline-seconds","420"]
```

After administrative validation and before any Git child, the verifier exclusively creates
the verifier-start claim. It has exactly `schema_version`, `treatment_id`, `claim_sha256`,
`open_freeze_commit_sha`, `registration_content_sha256`, and `verifier_argv_sha256`. An
existing verifier-start claim blocks every later verifier process before network access, so
the supervisor claim cannot be reused after a crash to obtain another query. A direct
verifier invocation without the exact lifecycle claim also fails before creating its start
claim or accessing the network.

The verifier makes at most three attempts. The exact Git child argv is:

```json
["C:\\Users\\User\\anaconda3\\Library\\bin\\git.exe","-c","credential.interactive=never","-c","core.askPass=","-c","credential.helper=","ls-remote","--tags","https://github.com/bansarinejad/arc3-crosslevel-voi.git","refs/tags/action-qbc-v8-open-diagnostic-freeze-v1"]
```

The Git child cwd is the verified non-repository directory `D:\kaggle competitions`; both
`D:\kaggle competitions\.git` and
`D:\kaggle competitions\arc3-v8-nonexistent-home` must be absent. Its environment is
constructed from an empty mapping with exactly:

```text
SystemRoot=C:\Windows
WINDIR=C:\Windows
COMSPEC=C:\Windows\System32\cmd.exe
TEMP=C:\Users\User\AppData\Local\Temp
TMP=C:\Users\User\AppData\Local\Temp
PATH=C:\Users\User\anaconda3\Library\mingw64\bin;C:\Users\User\anaconda3\Library\usr\bin;C:\Users\User\anaconda3\Library\bin;C:\Windows\System32;C:\Windows
PATHEXT=.COM;.EXE;.BAT;.CMD
HOME=D:\kaggle competitions\arc3-v8-nonexistent-home
XDG_CONFIG_HOME=D:\kaggle competitions\arc3-v8-nonexistent-home
GIT_CONFIG_NOSYSTEM=1
GIT_CONFIG_GLOBAL=NUL
GIT_CONFIG_COUNT=0
GIT_TERMINAL_PROMPT=0
GCM_INTERACTIVE=Never
GIT_ASKPASS=NUL
SSH_ASKPASS=NUL
```

No inherited member exists, including any other `GIT_*`, `GCM_*`, config injection,
repository/object/alternate directory, URL rewrite, pager, editor, proxy, askpass, or SSH
variable. The verifier hashes the canonical sorted environment into the receipt before
spawn. Together with the literal URL and neutral cwd, this makes global/system/local
`url.*.insteadOf` and the observed host `GIT_CONFIG_COUNT` injection inoperative. Every
Windows Git subprocess used by supervisor/verifier, including local HEAD/tag plumbing, uses
this same environment and neutral cwd; local commands address the repository only with a
literal `-C D:\kaggle competitions\arc3-crosslevel-voi` argv. The online child starts in a new
Windows process group and a private Windows Job Object created with
`JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`; the standard-library `ctypes` launcher creates the
process suspended, assigns it to the job, then resumes it, so no descendant can escape
before assignment. Stdout is streamed with a 4,096-byte cap and stderr with a
16,384-byte cap. Cap overflow is immediate nonretryable failure. Each attempt has a
120-second monotonic deadline. Timeout, either stream cap, or the 420-second overall
deadline while a Git child is live forces the same registered cleanup: invoke
`taskkill.exe /PID <pid> /T /F` under the shared 30-second cleanup deadline, then call
`TerminateJobObject` if needed, wait for the parent handle, and query the Job Object until
`ActiveProcesses == 0`. A hung taskkill child is itself terminated before the remaining
deadline is used for the Job Object fallback. A signaled parent plus zero active job
processes is the registered process-tree-gone predicate. Cleanup failure is immediate and
nonretryable; closing the kill-on-close job handle remains mandatory. A
timeout records synthetic exit 124 only after cleanup passes. If the child was already
signaled when a cap/deadline was detected, no kill is needed but the zero-active-process
query must still pass. A retry requests `time.sleep(15)` and the next monotonic start must be at least 15,000
milliseconds after the prior end. Durations are nonnegative integer milliseconds computed
by floor division of monotonic nanosecond differences. No attempt starts unless its full
deadline and cleanup reserve fit the total verifier deadline of 420 seconds.

Success requires exit zero and stdout bytes exactly:

```text
<O8_COMMIT>\trefs/tags/action-qbc-v8-open-diagnostic-freeze-v1\n
```

The tag must also resolve locally as a lightweight tag at the same lowercase 40-hex O8
commit. Empty stdout with exit `0`, synthetic timeout `124`, or Git failure `128` is
retryable. Any nonempty unexpected stdout, wrong ref, wrong hash, multiple lines, peeled
ref, malformed bytes, or other exit code terminates immediately. Stderr is hashed and
bounded but cannot establish success.

The verifier always attempts an exclusive canonical receipt publication at:

```text
D:\kaggle competitions\arc3-crosslevel-voi-action-qbc-v8-remote-verification.json
```

The four Windows artifacts and corresponding Linux source paths are:

```text
/mnt/d/kaggle competitions/arc3-crosslevel-voi-action-qbc-v8-remote-verification-claim.json
/mnt/d/kaggle competitions/arc3-crosslevel-voi-action-qbc-v8-remote-verifier-start-claim.json
/mnt/d/kaggle competitions/arc3-crosslevel-voi-action-qbc-v8-remote-verification.json
/mnt/d/kaggle competitions/arc3-crosslevel-voi-action-qbc-v8-remote-verification-supervisor.json
```

The receipt contains exactly:

```text
schema_version
treatment_id
claim_sha256
verifier_start_claim_sha256
open_freeze_commit_sha
open_freeze_tag
registration_content_sha256
remote_url
ref
python
git
taskkill
policy
attempts
status
selected_attempt
total_duration_milliseconds
```

Each tool object has exactly `path`, `version`, and `sha256`. `policy` has exactly
`max_attempts=3`, `attempt_timeout_seconds=120`, `retry_delay_seconds=15`,
`overall_deadline_seconds=420`, `verifier_child_deadline_seconds=440`,
`supervisor_deadline_seconds=480`, `supervisor_receipt_reserve_seconds=10`,
`stdout_cap_bytes=4096`, `stderr_cap_bytes=16384`, and
`child_cleanup_timeout_seconds=30`, `windows_job_kill_on_close=true`,
`git_child_cwd="D:\\kaggle competitions"`, and `git_environment` equal to the exact
sixteen-key object above. Each attempt has exactly `attempt_index`, `exit_code`,
`classification`, `timed_out`, `duration_milliseconds`, `stdout_size_bytes`,
`stdout_sha256`, `stdout_base64`, `stderr_size_bytes`, `stderr_sha256`, `stderr_base64`, and
`child_cleanup_passes`. Base64 encodes the complete bounded bytes. `selected_attempt` is the
one-based successful attempt or null. Status is exactly `verified` or `failed`.
`classification` is exactly `verified`, `retryable_empty_exit_0`,
`retryable_timeout_124`, `retryable_git_128`, `unexpected_output`, `unexpected_exit`,
`stdout_limit`, `stderr_limit`, `child_cleanup_failed`, `spawn_error`, or
`overall_deadline`. `exit_code` is null only for spawn failure; `child_cleanup_passes` is
non-null on every timeout, stdout-limit, stderr-limit, or overall-deadline classification
and null otherwise.

The supervisor captures the verifier child's complete stdout and stderr with the same
4,096/16,384-byte caps. It gives the verifier 440 monotonic seconds; any timeout or cap while
the verifier is live invokes the same suspended-start private Job Object, registered
`/PID <pid> /T /F`, `TerminateJobObject`, zero-active-process predicate, and 30-second
maximum. The 480-second supervisor deadline includes child wait,
cleanup, validation, and canonical receipt publication; the last 10 seconds are reserved
for receipt publication and no child may remain live when that reserve begins. The
supervisor never retries or replaces its verifier child.

The supervisor receipt contains exactly:

```text
schema_version
treatment_id
claim_sha256
verifier_start_claim_sha256
open_freeze_commit_sha
registration_content_sha256
verifier_argv_sha256
verifier_exit_code
classification
timed_out
duration_milliseconds
stdout_size_bytes
stdout_sha256
stdout_base64
stderr_size_bytes
stderr_sha256
stderr_base64
child_cleanup_passes
remote_receipt_sha256
status
```

Its classification is exactly `verifier_completed`, `verifier_timeout_124`, `stdout_limit`,
`stderr_limit`, `child_cleanup_failed`, `spawn_error`, `remote_receipt_missing`, or
`remote_receipt_invalid`. `verifier_exit_code` is null only for spawn error and is synthetic
124 for a passed timeout cleanup. `child_cleanup_passes` is non-null for timeout or either
cap and null otherwise. `verifier_start_claim_sha256` is SHA-256 of raw claim bytes when
that path exists and is null only when the verifier never created it; an invalid existing
claim therefore retains its raw hash. `remote_receipt_sha256` follows the same exists/hash
rule and is null only when no receipt path exists. Stream sizes, hashes, and Base64 values
always describe captured bytes, including the empty byte string after spawn failure.
`status` is `completed` only when a canonical matching remote
receipt exists and verifier exit zero corresponds to remote status `verified` or verifier
exit one corresponds to remote status `failed`; otherwise it is `failed`.

Claim and receipt JSON use sorted-key compact ASCII encoding with no NaN or final line feed.
Windows durability for every claim and receipt requires same-directory exclusive staging,
file flush and `os.fsync`,
reopen/hash/canonical validation, then an exclusive destination create/link; it does not
claim POSIX directory fsync. The supervisor is invoked exactly once and the verifier child
is started at most once; neither is retried, repaired, or replaced. A failed or missing
receipt after lifecycle-claim creation prevents A and B and is finalized administratively.

If supervisor validation or claim publication fails before the exclusive Windows lifecycle
claim exists, the irreversible lifecycle never starts: no verifier, driver, finalizer, or R8
is permitted, and a new preregistration is required. Only if the claim exists after the sole
supervisor process terminates does the one-shot Linux lifecycle driver in section 8 run and
invoke this exact arm child command first:

```json
["/usr/bin/timeout","--foreground","--signal=TERM","--kill-after=5s","120s","/usr/bin/python3","-I","-B","scripts/prepare_action_qbc_v8_open.py","arm","--repository-root",".","--registration","artifacts/action_qbc_v8_open_registration.json","--execution-root","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open","--preparation-receipt","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/preparation-receipt.json","--windows-claim","/mnt/d/kaggle competitions/arc3-crosslevel-voi-action-qbc-v8-remote-verification-claim.json","--windows-verifier-start-claim","/mnt/d/kaggle competitions/arc3-crosslevel-voi-action-qbc-v8-remote-verifier-start-claim.json","--windows-remote-receipt","/mnt/d/kaggle competitions/arc3-crosslevel-voi-action-qbc-v8-remote-verification.json","--windows-supervisor-receipt","/mnt/d/kaggle competitions/arc3-crosslevel-voi-action-qbc-v8-remote-verification-supervisor.json","--arm-receipt","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/arm-receipt.json"]
```

It validates and exclusively copies every available complete artifact to
`remote-verification-claim.json`, `remote-verifier-start-claim.json`,
`remote-verification.json`, and `remote-verification-supervisor.json` in the execution root,
then publishes an arm receipt with exactly `schema_version`, `treatment_id`,
`open_freeze_commit_sha`, `registration_content_sha256`, `preparation_receipt_sha256`,
`remote_claim_sha256`, `remote_verifier_claim_sha256`, `remote_receipt_sha256`,
`remote_supervisor_receipt_sha256`, and `status`. All later consumers use only these
immutable Linux copies. A remote hash is null only when its source artifact is absent;
status is `armed` only for a complete, mutually matching set whose supervisor is completed
and whose remote receipt is verified, and otherwise `failed`. A canonical supervisor
failure (timeout, cap, spawn, cleanup, or a valid remote failed status) maps to
`remote_verification_failed`. Expected absence of a verifier-start claim or remote receipt
that is explicitly recorded by a canonical matching supervisor-failure receipt is part of
that stage and is exempt from `remote_receipt_invalid`. Otherwise, a missing, malformed,
noncanonical, or cross-mismatched remote or supervisor receipt maps to
`remote_receipt_invalid`; a malformed or missing arm receipt
maps to `arm_receipt_invalid`. These mappings are deterministic inputs to the lifecycle
driver and finalizer.

After arm, remote/fetch/push Git, GitHub, HTTP, other network, environment build, checkout,
source mutation, tests, and verifier commands are forbidden until finalization finishes.
Only the exact registered read-only local Git plumbing needed for HEAD, tag, tree, blob, and
status validation is permitted.

## 8. Exactly two scientific processes

Processes are deterministic observations A and B of one v8 treatment. Each has its own
fresh clone, `.venv`, output directory, and process label. The only allowed ordering is:

```text
A -> validate A -> B -> validate B -> finalize once
```

After the supervisor returns, authority starts exactly one Linux lifecycle driver using the
section 6 outer WSL launcher and authority cwd. Its exact inner argv is:

```json
["/usr/bin/python3","-I","-B","scripts/execute_action_qbc_v8_open_lifecycle.py","execute","--repository-root",".","--registration","artifacts/action_qbc_v8_open_registration.json","--execution-root","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open","--preparation-receipt","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/preparation-receipt.json","--windows-claim","/mnt/d/kaggle competitions/arc3-crosslevel-voi-action-qbc-v8-remote-verification-claim.json","--remote-claim","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/remote-verification-claim.json","--remote-verifier-claim","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/remote-verifier-start-claim.json","--remote-receipt","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/remote-verification.json","--remote-supervisor-receipt","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/remote-verification-supervisor.json","--arm-receipt","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/arm-receipt.json","--driver-claim","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/lifecycle-driver-claim.json","--ledger","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/lifecycle-ledger.json"]
```

The standard-library-only driver first validates only its fixed argv, its own exact O8
blob/raw bytes, the registration's content identity, and the existing raw Windows lifecycle
claim, then exclusively creates its driver claim before any other gating. The Linux
remote-copy paths are required to be absent at that point; arm creates them. All deeper
preparation, authority, registration, and remote-artifact validation happens after the
driver claim and therefore ends in a ledger/finalizer outcome rather than an unrecorded
preclaim exit.

The driver claim has exactly `schema_version`, `treatment_id`, `open_freeze_commit_sha`,
`registration_content_sha256`, `remote_claim_sha256`, and `driver_argv_sha256`. An existing
claim blocks a second driver invocation. The driver owns and directly observes the arm,
runner, validator, and finalizer child processes; operators never supply exit codes, stages,
or ordering facts.

The driver first invokes the exact arm command in section 7. It starts A only after arm exits
zero and the arm receipt independently validates as `armed`; starts A's validator only after
A exits zero with an output; starts B only after A's validator exits zero and its immutable
receipt validates as `valid`; and starts B's validator only after B exits zero with an
output. It never retries any child. It records each returned child before considering the
next. Any failed dependency selects the first registered administrative stage and skips all
later scientific children.

The arm, each validator, and the finalizer are bounded by registered GNU `timeout` wrappers
of 120, 300, and 300 seconds respectively, each using `--foreground --signal=TERM
--kill-after=5s`. Their timeout exit is 124. Every local Git child owned by the driver uses a
60-second subprocess timeout with process-group TERM and five-second KILL cleanup. The
driver has one 8,400-second monotonic deadline and reserves the final 1,200 seconds for
ledger publication, finalizer, emergency rendering, and the first Git-publication attempt;
it starts no child unless that child's full registered bound fits before the reserve. A
deadline or caught driver-control exception selects `lifecycle_driver_failed` and enters the
ledger/finalizer path without another scientific start. These
bounds are administrative and do not alter the frozen scientific 2,100/2,400/2,700-second
limits. The driver deadline is enforced by its own standard-library control loop, not an
outer watchdog; the exogenous driver-process failure limitation in section 7 therefore
applies and is not represented as a successful v8 terminal.

Before finalization, the driver exclusively publishes one canonical lifecycle ledger with
exactly:

```text
schema_version
treatment_id
open_freeze_commit_sha
registration_content_sha256
driver_claim_sha256
arm_exit_code
arm_receipt_sha256
sequence
process_a
process_b
stage
```

Each ledger process object has exactly `label`, `cwd`, `runner_argv_sha256`,
`runner_exit_code`, `validator_argv_sha256`, `validator_exit_code`,
`start_claim_sha256`, `validator_claim_sha256`, `validation_receipt_sha256`, and
`output_sha256`. A fact is null exactly when its command or artifact was not reached.
`sequence` is one of the ordered prefixes of
`["arm_returned","process_a_runner_returned","process_a_validator_returned",`
`"process_b_runner_returned","process_b_validator_returned"]`; each member is appended
only after that child returns and its exit code is captured. `stage` is null only after all
five members exist with zero exits and both validation receipts are valid; otherwise it is
the first applicable registered stage. The ledger is the sole authority for exit codes and
order. If ordinary Python failure prevents a normal ledger, the driver's `finally` path
attempts one minimal canonical ledger with the evidence reached and stage
`lifecycle_ledger_invalid`; no child is retried.

Immediately after the exclusive ledger publication attempt, the same driver makes one
process-start attempt for the exact finalizer command in section 9 from authority. This
happens under every arm or process outcome and is never retried. A ledger-publication
failure is passed only by the absence or invalidity of the fixed ledger path; it is not an
operator-selected stage. The driver then selects the finalizer bundle or creates the exact
emergency bundle and runs the registered Git-only publisher as section 9 specifies.

Each process runs the registered v8 runner under GNU `timeout` with the frozen 2,100/2,400/
2,700-second limits. No process can be restarted, repaired, resumed, or substituted.

The common scientific argv template is:

```json
["/usr/bin/timeout","--foreground","--signal=TERM","--kill-after=15s","2700s",".venv/bin/python3","-I","-B","scripts/run_action_qbc_v8_open_diagnostic.py","--repository-root",".","--registration","artifacts/action_qbc_v8_open_registration.json","--arm-receipt","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/arm-receipt.json","--driver-claim","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/lifecycle-driver-claim.json","--label","<LABEL>","--start-claim","<START_CLAIM>","--prior-validation-receipt","<PRIOR_VALIDATION_OR_NULL>","--compute-deadline-seconds","2100","--wall-time-seconds","2400","--output","<OUTPUT_PATH>"]
```

The substitutions and driver cwd are exactly:

| Label | Cwd | Start claim | Prior validation | Output |
|---|---|---|---|---|
| `A` | `/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/processes/process-a` | `/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/process-a-start-claim.json` | `null` | `/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/processes/process-a-output/open/action_qbc_v8_open_diagnostic.json` |
| `B` | `/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/processes/process-b` | `/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/process-b-start-claim.json` | `/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/process-a-validation.json` | `/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/processes/process-b-output/open/action_qbc_v8_open_diagnostic.json` |

Each output parent is a distinct empty owner-controlled mode-0700 plain directory created
during preparation. Using only standard-library and local read-only Git code, the runner
first validates its fixed argv, clone, registration, immutable arm receipt and
lifecycle-driver claim, label/prior
dependency, paths, deadlines, tracked raw bytes, allowed `.venv`, and empty output directory.
For B, the prior A validation receipt must already be canonical and valid. Only then, and
before importing the v8 audit module, it exclusively creates and fsyncs its start claim.
Successful claim creation is the scientific start. An invocation that fails administrative
gating or encounters an existing claim makes zero scientific starts and cannot import the
audit module.

Each start claim has exactly `schema_version`, `treatment_id`, `label`,
`open_freeze_commit_sha`, `registration_content_sha256`, `arm_receipt_sha256`,
`lifecycle_driver_claim_sha256`, `scientific_argv_sha256`,
`prior_validation_receipt_sha256`, and `output_path`. The prior
hash is null only for A. Claim creation is exclusive; an existing claim permanently blocks
another start for that label. Exactly two label/path pairs exist, so a third scientific
start cannot acquire a registered claim. After a claim, any timeout, crash, fallback, or
pre-producer error consumes that process and cannot be retried.

After the claim, the runner imports the exact v8 audit module, follows section 4.3, and
publishes only a canonical internally validated payload by exclusive atomic creation. A
nonzero A exit, missing A output, or failed A validation prevents B.

The common validator argv template, run from the corresponding process clone, is:

```json
["/usr/bin/timeout","--foreground","--signal=TERM","--kill-after=5s","300s",".venv/bin/python3","-I","-B","scripts/validate_action_qbc_v8_open_payload.py","--repository-root",".","--registration","artifacts/action_qbc_v8_open_registration.json","--arm-receipt","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/arm-receipt.json","--driver-claim","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/lifecycle-driver-claim.json","--label","<LABEL>","--start-claim","<START_CLAIM>","--validator-claim","<VALIDATOR_CLAIM>","--validation-receipt","<VALIDATION_RECEIPT>","--payload","<OUTPUT_PATH>"]
```

For A the validator claim and receipt are respectively
`/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/process-a-validator-claim.json` and
`/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/process-a-validation.json`; B uses the
corresponding `process-b-...` paths.

The validator never calls the producer, finalizer, scene generator, compiler, planner,
selector, controller, or worker. It validates source/module origin, registration, arm
receipt, complete payload schema/semantics, canonical bytes, cap, and resource counters. It
creates an exclusive validator claim after administrative gating and before importing the
audit validator. The validator claim has exactly `schema_version`, `treatment_id`, `label`,
`lifecycle_driver_claim_sha256`, `start_claim_sha256`, `validator_argv_sha256`, and
`payload_sha256`. Its existence forbids a
second validator invocation. The canonical validation receipt has exactly
`schema_version`, `treatment_id`, `label`, `start_claim_sha256`,
`validator_claim_sha256`, `payload_path`, `payload_sha256`, `payload_size_bytes`, and
`status`, where status is `valid` or `invalid`. The validator attempts to publish this
receipt under either outcome and exits zero only for `valid`.

It is invoked once after each zero-exit runner with an existing output and is never retried.
B follows only a zero-exit A validator with status `valid`. Finalization follows the B
validator, or the first earlier condition that prevents progress. The finalizer validates
every available start/validator claim and validation receipt, including B's dependency on
the exact A validation-receipt SHA.

At most two scientific starts exist. There is no third start under any outcome, including a
timeout, fallback, crash, invalid payload, byte mismatch, publication failure, or operator
mistake.

## 9. Finalization and result boundary

The lifecycle driver makes exactly one start attempt for the standard-library-only finalizer
from authority after the lifecycle terminates, including an arm or A-side failure. At most
one finalizer process is actually invoked; spawn failure is handled by the emergency path.
When invoked, it validates
the preparation, remote artifacts, arm receipt, registration, source objects, raw
materialization, lifecycle ledger, any process payloads, complete v8 schema, resource
ledger, cap, process order, and exact A/B byte equality. It independently rederives the
scientific structural invariants and never imports a project module.

Its exact bounded argv from authority is:

```json
["/usr/bin/timeout","--foreground","--signal=TERM","--kill-after=5s","300s","/usr/bin/python3","-I","-B","scripts/finalize_action_qbc_v8_open_diagnostic.py","--repository-root",".","--registration","artifacts/action_qbc_v8_open_registration.json","--preparation-receipt","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/preparation-receipt.json","--remote-claim","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/remote-verification-claim.json","--remote-verifier-claim","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/remote-verifier-start-claim.json","--remote-receipt","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/remote-verification.json","--remote-supervisor-receipt","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/remote-verification-supervisor.json","--arm-receipt","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/arm-receipt.json","--driver-claim","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/lifecycle-driver-claim.json","--lifecycle-ledger","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/lifecycle-ledger.json","--process-a-start-claim","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/process-a-start-claim.json","--process-a-validator-claim","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/process-a-validator-claim.json","--process-a-validation-receipt","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/process-a-validation.json","--process-a","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/processes/process-a-output/open/action_qbc_v8_open_diagnostic.json","--process-b-start-claim","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/process-b-start-claim.json","--process-b-validator-claim","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/process-b-validator-claim.json","--process-b-validation-receipt","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/process-b-validation.json","--process-b","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/processes/process-b-output/open/action_qbc_v8_open_diagnostic.json","--bundle","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/finalization-bundle.json"]
```

The underlying-evidence first-applicable precedence is exactly:

```text
remote_verification_failed
remote_receipt_invalid
arm_receipt_invalid
registration_invalid
authority_identity_invalid
lifecycle_ledger_invalid
lifecycle_driver_failed
process_a_nonzero
process_a_output_missing
process_a_validation_failed
process_b_nonzero
process_b_output_missing
process_b_validation_failed
payload_byte_mismatch
```

The ledger stage can only agree with that independently rederived underlying stage.
Scientific fallbacks remain payload outcomes. Two disposition overrides are outside the
underlying order: `receipt_finalization_failed` overrides the exposed `stage` only when a
running finalizer cannot construct the machine terminal, and `finalizer_process_failed`
overrides it only when the driver must create an emergency bundle after the finalizer start
attempt. Every bundle separately records `underlying_stage`, so an earlier cause is never
erased. Collision tests apply this two-layer rule exactly; the two override stages never
compete with each other because only the driver can create the latter.

Each process object in a receipt or terminal contains exactly `label`, `output_path`,
`exit_code`, `validator_exit_code`, `start_claim`, `start_claim_sha256`, `validator_claim`,
`validator_claim_sha256`, `validation_receipt`, `validation_receipt_sha256`,
`payload_exists`, `payload_valid`, `payload_sha256`, and `payload_size_bytes`. A canonical
claim/receipt object is embedded when valid and otherwise null; its SHA is SHA-256 of raw
bytes whenever its path exists and null only when absent. This records acquired scientific
starts, one-shot validators, and B's exact A-receipt dependency.

The successful final receipt has exactly:

```text
schema_version
treatment_id
open_freeze_commit_sha
open_freeze_tag
registration_content_sha256
preparation_receipt
remote_verification_claim
remote_verifier_claim
remote_verification_receipt
remote_supervisor_receipt
arm_receipt
lifecycle_driver_claim
lifecycle_ledger
process_a
process_b
payloads_byte_identical
published_payload_path
published_payload_sha256
authorization
```

The administrative terminal has exactly the identity/receipt/process members through
`process_b`, plus `payloads_byte_identical`, `stage`, and `authorization`; it has no
published-payload members. It embeds every complete canonical preparation, remote, arm,
driver, ledger, process-claim, and validation object available. All authorization members
are false, and independently validated registration/O8 identities survive later failures.

### 9.1 Canonical finalization bundles and emergency ownership

The finalizer writes no repository path. It exclusively publishes one fsynced canonical
bundle at `/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/finalization-bundle.json`. A
normal bundle has exactly:

```text
schema_version
treatment_id
open_freeze_commit_sha
registration_content_sha256
disposition
stage
underlying_stage
files
authorization
content_sha256
```

`content_sha256` hashes canonical sorted-key compact ASCII JSON for the other fields.
`disposition` is `scientific_result` or `administrative_terminal`; `stage` and
`underlying_stage` are both null only for a scientific result. For a normal machine
terminal they are equal. For the doc-only `receipt_finalization_failed` override, stage is
that override and `underlying_stage` preserves the independently derived earlier cause or
is null when none existed. `files` is path-sorted, nonempty, and each object has exactly `path`,
`mode` (always `100644`), `size_bytes`, `sha256`, and `content_base64`. Base64 decodes to
the complete exact file bytes and every size/hash is rechecked independently. The bundle
itself uses compact sorted-key ASCII JSON with no NaN or final line feed. The finalizer exits
zero only after reopening and validating the exclusively created bundle.

For a valid byte-identical pair, `files` contains exactly:

```text
artifacts/action_qbc_v8_open_diagnostic.json
artifacts/action_qbc_v8_open_diagnostic_receipt.json
docs/action_qbc_v8_open_diagnostic_result.md
```

For a normal administrative terminal, it contains exactly:

```text
artifacts/action_qbc_v8_open_diagnostic_administrative_terminal.json
docs/action_qbc_v8_open_diagnostic_result.md
```

If the finalizer can render a deterministic document but cannot construct a canonical
machine terminal, it emits exactly the document path with stage
`receipt_finalization_failed`. No payload orphan or partial path set is permitted.

The result document is never hand-authored. The finalizer uses an O8-fixed Markdown template
and pure renderer whose source identities, template SHA-256, inputs, and expected bytes for
every normal stage and the valid-pair case are registered in `result_document_contract`.
Inputs are limited to canonical evidence and registered identities; no clock, hostname, or
free prose is emitted.

If the finalizer exits nonzero (including 124), cannot be spawned, or leaves a missing,
malformed, noncanonical, or identity-mismatched normal bundle, the driver does not invoke it
again. Instead the driver exclusively publishes
`/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/emergency-result-bundle.json`. The emergency
bundle has exactly:

```text
schema_version
treatment_id
open_freeze_commit_sha
registration_content_sha256
disposition
stage
underlying_stage
finalizer_exit_code
finalization_bundle_exists
finalization_bundle_sha256
lifecycle_ledger_exists
lifecycle_ledger_sha256
files
authorization
content_sha256
```

Its disposition is `administrative_terminal`, stage is `finalizer_process_failed`, and
`underlying_stage` is the independently derived earlier cause when determinable and
otherwise null. The emergency override never replaces this separate field.
`finalizer_exit_code` is null only for spawn failure. The existence Boolean describes the
normal destination; its raw SHA is present exactly when true. The ledger Boolean/hash use
the same exists/raw-SHA rule and therefore retain evidence for a missing or malformed
ledger. Its `files` contains only `docs/action_qbc_v8_open_diagnostic_result.md`, encoded
with the same exact file-object
schema. The driver uses a separately registered O8-fixed emergency template that records
the fixed identities, exit code, normal-bundle and ledger existence/hashes, exposed and
underlying stages, and false
authorization. Emergency bundle content hashing, exclusive creation, fsync, reopening, and
validation are identical to the normal bundle rules. Preparation requires both bundle paths
absent; neither producer overwrites, adopts, or deletes a preexisting destination.

Exactly one bundle is selected: a canonical emergency bundle always wins when it exists;
otherwise a canonical normal bundle is required. A valid normal bundle and an emergency
bundle cannot both be produced on a zero-exit finalizer path. The selected bundle is
immutable before any Git object or ref is written.

### 9.2 Worktree-independent R8 construction and authoritative tag

The driver constructs R8 without reading or changing authority's worktree or real index.
Preparation requires `/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/result-git-owner.json`
and `/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/result-git-work` to be absent. Before
the first publication attempt, the publisher exclusively creates the owner claim, then a
mode-0700 plain work directory and `.owner` marker whose bytes are the claim SHA. The claim
has exactly `schema_version`, `treatment_id`, `open_freeze_commit_sha`,
`registration_content_sha256`, `driver_claim_sha256`, `work_root`, and `owner_nonce`; the
nonce is SHA-256 of the preceding six canonical fields. If a crash occurs after the claim
but before directory/marker completion, a later publication-only invocation may complete
that initialization only after validating the claim, parent execution-root device/owner/
mode, absent-or-empty nonsymlink work path, and exact marker rule.

Attempt `i` in `{1,2,3}` uses only `result-git-work/index-i`, its Git-created
`index-i.lock`, and `result-tag-i`. Before every attempt, the publisher validates the owner claim, marker,
directory device/inode/owner/mode, then checks the final tag before touching scratch. An
exact final tag with an absent authority result branch is immediately revalidated/fsynced
and returned as idempotent success; no new scratch is created and leftover owned scratch is
irrelevant. A wrong final tag or present authority result branch fails closed. Only while
the final tag is absent does the publisher use dirfd-relative no-follow operations to remove
only those three regular owned scratch files if present. A `result-tag-i` with link count two
cannot reach this branch because its exact final link would already have returned success;
cleanup accepts only link count one, expected bytes, and an absent final tag. Any symlink,
directory, special file,
unexpected name, or ownership mismatch fails closed. It then exclusively creates a fresh
index through `read-tree`. Thus timeout or SIGKILL cannot strand a partial index/lock, and
no evidence, real index, Git object, ref, bundle, ledger, or foreign path is removed. A
later publisher invocation repeats the same verified cleanup and three fresh attempts.

Each Git child receives `GIT_INDEX_FILE` for the current attempt. The publisher constructs
the child environment from an empty mapping with exactly `PATH=/usr/bin:/bin`,
`HOME=/nonexistent`, `XDG_CONFIG_HOME=/nonexistent`, `LANG=C`, `LC_ALL=C`, `TZ=UTC`,
`GIT_CONFIG_NOSYSTEM=1`, `GIT_CONFIG_GLOBAL=/dev/null`,
`GIT_NO_REPLACE_OBJECTS=1`, the exact `GIT_DIR`, `GIT_WORK_TREE`, current
`GIT_INDEX_FILE`, and the six author/committer variables below. No inherited environment
member exists; in particular every other `GIT_*`, config-injection, object-directory,
replace, alternate-object, hook, pager, editor, or external-diff variable is absent. The
publisher validates the clone-local config against the exact bootstrap allowlist and fixes
this complete child environment:

```text
PATH=/usr/bin:/bin
HOME=/nonexistent
XDG_CONFIG_HOME=/nonexistent
LANG=C
LC_ALL=C
TZ=UTC
GIT_CONFIG_NOSYSTEM=1
GIT_CONFIG_GLOBAL=/dev/null
GIT_NO_REPLACE_OBJECTS=1
GIT_DIR=/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/authority/.git
GIT_WORK_TREE=/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/authority
GIT_INDEX_FILE=/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/result-git-work/index-<i>
GIT_AUTHOR_NAME=ARC3 v8 Result Bot
GIT_AUTHOR_EMAIL=arc3-v8-result@invalid.example
GIT_AUTHOR_DATE=2026-08-11T00:00:00+10:00
GIT_COMMITTER_NAME=ARC3 v8 Result Bot
GIT_COMMITTER_EMAIL=arc3-v8-result@invalid.example
GIT_COMMITTER_DATE=2026-08-11T00:00:00+10:00
```

It performs these registered plumbing steps, replacing placeholders only with independently
validated bundle values:

```json
[
  ["/usr/bin/git","read-tree","<O8_COMMIT>"],
  ["/usr/bin/git","hash-object","-w","--stdin"],
  ["/usr/bin/git","update-index","--add","--cacheinfo","100644,<FILE_BLOB>,<FILE_PATH>"],
  ["/usr/bin/git","write-tree"],
  ["/usr/bin/git","-c","commit.gpgSign=false","-c","i18n.commitEncoding=UTF-8","commit-tree","<RESULT_TREE>","-p","<O8_COMMIT>"],
  ["/usr/bin/git","diff-tree","--no-commit-id","--name-status","-r","-z","<O8_COMMIT>","<R8_COMMIT>"],
  ["/usr/bin/git","cat-file","-p","<R8_COMMIT>"]
]
```

The hash/update-index pair repeats once per path in sorted order. `hash-object` stdin is the
decoded registered file bytes. `commit-tree` stdin is exactly
`Record action-QBC v8 open diagnostic result\n`; its sole parent must be O8 and its tree
must differ from O8 only by additions for the selected path set. Explicit config isolation,
disabled signing, UTF-8 binding, fixed locale/time zone, and fixed author/committer
environment make the commit bytes independently reconstructable. Authority creates only the
single lightweight result tag; the result branch is a later publication alias, not part of
the authoritative offline boundary. No Git ref command or hook is run.

After the preceding exact-final-tag precheck, attempt `i` exclusively creates
`result-git-work/result-tag-i`, writes exactly `<R8_COMMIT>\n` with mode 0444, flushes and
fsyncs it, then reopens and validates its type, owner, device, link count, bytes, and SHA.
It opens authority's `.git/refs/tags` through no-follow dirfd traversal, requires the result
tag absent or an exact same-owner regular 41-byte `<R8_COMMIT>\n` file, and requires the
authority result branch absent. For an absent tag it uses same-device `os.link` with
source/destination dirfds and `follow_symlinks=False` to create the final tag atomically,
then fsyncs the tag directory and revalidates both Git resolution and `cat-file -t=commit`.
An exact existing tag is idempotent success after directory fsync. A crash before the link
leaves only verified-owned scratch that the next attempt may remove; a crash after the link
leaves the complete exact tag. Any wrong tag, branch, symlink, special file, ownership/mode/
link-count mismatch, cross-device condition, or unexpected ref lock fails closed and is
never cleaned. Injected `reference-transaction` hooks are irrelevant and tests prove they
are not executed. Object writes are content-addressed and repeatable.
`result_git_max_attempts` is three per publisher invocation, and the driver makes at most
one such invocation within its reserve.
If they do not complete, the exact publication-only command is:

```json
["/usr/bin/python3","-I","-B","scripts/execute_action_qbc_v8_open_lifecycle.py","publish","--repository-root",".","--registration","artifacts/action_qbc_v8_open_registration.json","--driver-claim","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/lifecycle-driver-claim.json","--lifecycle-ledger","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/lifecycle-ledger.json","--finalization-bundle","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/finalization-bundle.json","--emergency-bundle","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/emergency-result-bundle.json"]
```

Publication-only mode may be repeated after the driver returns. It validates the immutable
claim, bundle, O8 objects, candidate tree/commit, and refs. A canonical ledger is normally
required. Absence or malformation is accepted only when either (a) the selected normal
bundle independently rederives `underlying_stage=lifecycle_ledger_invalid` and has the exact
registered file set for its exposed stage, including the doc-only
`receipt_finalization_failed` override, or (b) the selected emergency bundle's ledger
existence/hash fields exactly describe the raw path and its fixed emergency document records
the same evidence. No other ledger bypass exists. Publication-only mode cannot invoke arm,
runner, validator, finalizer, renderer, or any network command, and cannot change evidence.
R8 is a direct child of O8 with exactly one of the three path sets above and is immutable
before online publication.

From explicit Ubuntu, local object transfer into the original checkout uses exactly:

```json
[
  ["/usr/bin/git","-C","/mnt/d/kaggle competitions/arc3-crosslevel-voi","fetch","--no-tags","file:///var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/authority","refs/tags/action-qbc-v8-open-diagnostic-result-v1:refs/heads/action-qbc-v8-open-diagnostic-result"],
  ["/usr/bin/git","-C","/mnt/d/kaggle competitions/arc3-crosslevel-voi","fetch","--no-tags","file:///var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/authority","refs/tags/action-qbc-v8-open-diagnostic-result-v1:refs/tags/action-qbc-v8-open-diagnostic-result-v1"]
]
```

The target branch is not the original checkout's current branch. Only after both local refs
resolve to R8 may Windows Git execute:

```json
[
  ["C:\\Users\\User\\anaconda3\\Library\\bin\\git.exe","push","origin","refs/heads/action-qbc-v8-open-diagnostic-result:refs/heads/action-qbc-v8-open-diagnostic-result"],
  ["C:\\Users\\User\\anaconda3\\Library\\bin\\git.exe","push","origin","refs/tags/action-qbc-v8-open-diagnostic-result-v1:refs/tags/action-qbc-v8-open-diagnostic-result-v1"],
  ["C:\\Users\\User\\anaconda3\\Library\\bin\\git.exe","-c","credential.interactive=never","ls-remote","--refs","origin","refs/heads/action-qbc-v8-open-diagnostic-result","refs/tags/action-qbc-v8-open-diagnostic-result-v1"]
]
```

The final stdout contains exactly the R8 branch and tag records at the same lowercase
40-hex commit. Online publication may be retried only after immutable R8 exists; it cannot
change evidence. There is no result-file copy, worktree cleanup, checked-out-branch update,
second finalizer, document edit, or scientific command.

## 10. Required gates and prohibited prefreeze work

Before O8, automated tests must cover at least:

- exact v7 module/blob/registration anchors;
- exact P8/O8 ancestry and allowlists;
- object-derived manifests under CRLF/mixed worktree materialization;
- raw Linux checkout equality to every committed blob;
- byte-exact section-4 source transformation, reverse transformation, AST/function parity,
  and rejection of every extra source change;
- every exact registration, payload, preparation, claim, remote, arm, process, final-receipt,
  and terminal key-set mutation;
- truthful process-invariant v8 identity with no label/root/output leakage;
- zero producer calls on precondition failure and at most one on every runner branch;
- normal, fallback, cap-minus-one, cap, and cap-plus-one payloads;
- injected `GlobalFallbackRequired` and generic exceptions at each of the six frozen O7 try
  operations (pre-producer deadline, producer, post-producer deadline, finalizer, first
  validation, canonical encoding), plus proof that staged-byte validation remains outside
  that boundary and administrative;
- remote success on attempts one through three, retry timing, timeout, Git 128, malformed
  output, byte-cap overflow, wrong tag/hash, multiple lines, durable claim after crash,
  process-tree cleanup for every forced termination, supervisor timeout/caps/receipt reserve,
  supervisor and verifier-start claim exclusivity, neutral Git cwd, complete Windows Git
  environment scrubbing, hostile inherited `GIT_CONFIG_COUNT`/`url.*.insteadOf`, and
  prohibition of a second online invocation;
- preparation raw-audit ordering, two-attempt cleanup ownership, `.venv` exclusion, arm copy
  immutability, atomic process/output promotion, preparation failure nullability, explicit
  Ubuntu launcher/tool rejection, independent post-preparation receipt validation, and no
  scientific start without a valid arm receipt;
- A validation before B, B validation before finalization, no validator retry, no B after
  invalid A, no third start, driver-claim exclusivity, every legal ledger prefix, and
  finalizer rejection of operator-supplied or inconsistent exit/order evidence;
- no write or mutation to a v7 path, no access to a v7 runtime/output root, and no v7 result
  backfill; read access is limited to the frozen v7 audit/reference modules, registration,
  and registered transitive P8 substrate;
- registration/commit identities retained after a materialization failure;
- every administrative precedence collision, pair equality, finalizer one-shot behavior,
  finalizer timeout/spawn failure, normal/emergency bundle exclusivity and hashing, terminal
  path exclusivity, deterministic normal/emergency result-document bytes, dirty-authority
  tolerance, underlying-stage/disposition-override collisions, hostile inherited `GIT_*`
  scrubbing, verified-owned partial-index/lock recovery, crash-before/after-link authoritative
  tag recovery, ignored executable `reference-transaction` hooks, idempotent publication-only
  recovery, and exact R8 object/transfer transaction;
- offline import/help, deterministic replay, and zero remote/network command after arming.

The exact frozen gate argv arrays are:

```json
[
  ["uv","run","--frozen","--extra","dev","pytest","-q","tests/test_action_qbc_v7_audit.py","tests/test_action_qbc_v8_audit.py","tests/test_action_qbc_v8_lifecycle.py","tests/test_action_qbc_v8_registration.py"],
  ["uv","run","--frozen","--extra","dev","ruff","check","src/arc3_voi/action_qbc_v8_audit.py","scripts/build_action_qbc_v8_open_registration.py","scripts/execute_action_qbc_v8_open_lifecycle.py","scripts/finalize_action_qbc_v8_open_diagnostic.py","scripts/prepare_action_qbc_v8_open.py","scripts/reconstruct_action_qbc_v8_open_registration.py","scripts/run_action_qbc_v8_open_diagnostic.py","scripts/supervise_action_qbc_v8_remote_tag.py","scripts/validate_action_qbc_v8_open_payload.py","scripts/verify_action_qbc_v8_remote_tag.py","tests/test_action_qbc_v8_audit.py","tests/test_action_qbc_v8_lifecycle.py","tests/test_action_qbc_v8_registration.py"],
  ["uv","run","--frozen","--extra","dev","mypy","--strict","src/arc3_voi/action_qbc_v8_audit.py"]
]
```

The v7 registration test is run only in an O7 checkout because its purpose is to reject any
post-O7 tree.

Before O8, tests may use stubs, monkeypatches, synthetic payloads, and registered public
data-only values. They must not invoke the complete v8 runner, call the full frozen
scientific producer, generate an extension-scene scientific snapshot, create the execution
root or remote receipt, or write any v8 result path. No live remote rehearsal is permitted
after P8. Required P8/O8 publication pushes and read-only remote-ref verification are
allowed; the next execution-phase online mechanism is the single registered O8 supervisor.

## 11. Git boundaries, tags, and claims

V8 defines three irreversible Git boundaries. P8 and O8 are mandatory; R8 exists only when
the Windows lifecycle claim was created and the registered driver reaches bundle
publication. A preclaim or exogenous driver-process failure explicitly stops at O8 as
section 7 states and cannot be described as an R8 result.

1. P8 is the direct child of R7 and adds only this document. Its lightweight tag is
   `prereg-action-qbc-v8-open-bounded-remote-verification-v1`.
2. O8 is the direct child of P8 and adds only the fifteen paths in section 5. Its lightweight
   tag is `action-qbc-v8-open-diagnostic-freeze-v1`.
3. R8 is the direct child of O8 and adds only one path set from section 9. Its lightweight
   tag is `action-qbc-v8-open-diagnostic-result-v1`.

Every tag is lightweight, immutable, pushed, and independently verified. Tags are never
moved, replaced, or deleted.

The result may report the registered public diagnostic values and whether A/B were
byte-identical. It may not claim unseen generalization, calibrated Bayesian inference,
runtime readiness, leaderboard improvement, causality of an individual compound-selector
component, or permission for sealed/lockbox work. A failed or incomplete v8 result remains
publishable negative or administrative evidence and cannot be tuned away.
