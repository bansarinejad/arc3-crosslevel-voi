# Action-QBC v8 P8v7 consumed-lifecycle recovery preregistration

Correction freeze: 18 August 2026 (Australia/Sydney)

Status: prospective preregistration-only administrative recovery. O8v3 was
constructed, frozen, published to the already-PUBLIC repository, verified by its
sole credential-disabled anonymous two-ref query, bootstrapped, prepared, and
irreversibly claimed. Its Windows verifier completed with a canonical failed
receipt before any remote attempt. Its mandatory Linux lifecycle then stopped
administratively before science, produced a valid emergency bundle, and failed
to publish a result because of a second frozen argv contradiction. One later
registered publication-only invocation reproduced that contradiction. No
scientific start, scientific import, payload, normal finalization bundle, result
Git owner, result work root, R8 object, result branch, or result tag exists.

## 1. Authority, incorporation, and immutable history

This document corrects and otherwise incorporates the complete protocol through:

    docs/experiment_amendment_2026-08-18_action_qbc_v8_open_bounded_remote_verification_v6_runner_manifest_key_recovery.md

Every P8v6 scientific, schema, timing, resource, claim, no-cleanup, no-retry,
evidence, failure-classification, public-state, and result-publication
requirement remains binding unless this document expressly replaces an
administrative lineage, ref, path, expected-mode, snapshot-order, or
publisher-argv-comparison value. P8v6 and O8v3 are immutable history. Nothing in
this document repairs, deletes, chmods, retries, reclassifies, or rewrites them.

P8v6 is:

    commit:              61cebe90a2f4f7c78ec45119de53a482ed13a655
    tree:                65695876c44eeb8cac5437149384071f88ff6018
    tag:                 prereg-action-qbc-v8-open-bounded-remote-verification-v6
    document:            docs/experiment_amendment_2026-08-18_action_qbc_v8_open_bounded_remote_verification_v6_runner_manifest_key_recovery.md
    document Git blob:   5e870ed0bbbff6fcb4352f6e914d870254773f68
    document SHA-256:    0ba4cc55ca2b31433bc458972ffc32d87f84b610673fa22ed2b4dd4a8bfc1a41
    document byte count: 32370

O8v3 is P8v6's direct exact-fifteen-addition child:

    commit: 5725395a850627fae10e8bb8b27083ccf63b6ec7
    tree:   7d38de8f5cec16cab92c9d3b757a218e8e490272
    tag:    action-qbc-v8-open-diagnostic-freeze-v3

The existing lineage branch is:

    action-qbc-v8-prereg

The branch and freeze-v3 resolve to O8v3. Every incorporated earlier tag remains
at its already frozen commit. No operator may move, replace, delete,
force-update, re-execute, or anonymously reverify any historical ref.

P8v7 must be O8v3's direct child. Its direct diff deletes exactly the same
fifteen paths O8v3 added and adds only this recovery document:

    docs/experiment_amendment_2026-08-18_action_qbc_v8_open_bounded_remote_verification_v7_consumed_lifecycle_recovery.md

Consequently P8v7's tree must equal the P8v6 tree plus this one P8v7 document by
path, mode, Git blob, raw SHA-256, and byte count. The net P8v6-to-P8v7 diff is
exactly the one P8v7 document addition. P8v7 modifies, deletes, renames, or adds
no other path. Its recursive Git tree inventory contains exactly 233 entries.
Its immutable lightweight tag is:

    prereg-action-qbc-v8-open-bounded-remote-verification-v7

P8v7_COMMIT, P8v7_TREE, P8v7_DOCUMENT_GIT_BLOB_SHA1,
P8v7_DOCUMENT_SHA256, and P8v7_DOCUMENT_BYTE_COUNT mean identities derived from
these exact committed bytes after P8v7 exists. They are not operator choices and
must remain symbolic in this document.

O8v4 must be P8v7's direct child and add exactly the fifteen paths in section 8.
It may modify, delete, or rename no P8v7 path. Its recursive Git tree inventory
contains exactly 248 entries. Its immutable lightweight tag is:

    action-qbc-v8-open-diagnostic-freeze-v4

The stable result branch remains:

    action-qbc-v8-open-diagnostic-result

The only prospective result tag for the corrected lifecycle is:

    action-qbc-v8-open-diagnostic-result-v4

The action-QBC v8 result-v1, result-v2, and result-v3 tags remain absent and must
never be created, backfilled, or accepted:

    action-qbc-v8-open-diagnostic-result-v1
    action-qbc-v8-open-diagnostic-result-v2
    action-qbc-v8-open-diagnostic-result-v3

This statement does not concern any v7 result ref. In particular, O8v3's valid
emergency bundle does not authorize a post-hoc action-QBC v8 result-v3 after
this preregistration.

The linear lineage is:

    R7 -> P8v1 -> P8v2 -> P8v3 -> P8v4 -> O8v1 -> P8v5 -> O8v2 -> P8v6 -> O8v3 -> P8v7 -> O8v4 -> R8v4

R8v4 exists only if the new O8v4 lifecycle reaches its registered result
publication boundary. No R8v3 exists.

## 2. O8v3 construction and PUBLIC publication disclosure

O8v3 was constructed from P8v6 with exactly the registered fifteen additions.
Its generated registration had these identities:

    path:             artifacts/action_qbc_v8_open_registration.json
    byte count:       209880
    final LF:         absent
    file SHA-256:     986393466859841ab68089d8898f496bbf597d183828b890120c05674da5a79d
    content SHA-256:  6eba981794f057bd8c1b183b7aaa6705faef08f2ca312eed2b69710b3c4d74a6
    Git blob:         b4eb817de0b5ec21caed54f330b4f37b604b031b
    source manifest:  55090430387deafadb2b06e1548a9b5192be190677176a743946def4ca54e273

The producer, byte reconstruction, frozen pytest, Ruff, strict-mypy, source,
tree, index, and post-commit gates succeeded. Frozen pytest reported 577 passed
and 28 skipped. O8v3 and freeze-v3 were published by the one registered
non-force atomic push to the already-PUBLIC repository.

The next protocol network action was O8v3's sole credential-disabled anonymous
two-ref query. It returned exit code 0 and empty stderr. Its stdout contained
exactly these two LF-terminated ASCII records, each once:

    5725395a850627fae10e8bb8b27083ccf63b6ec7	refs/heads/action-qbc-v8-prereg
    5725395a850627fae10e8bb8b27083ccf63b6ec7	refs/tags/action-qbc-v8-open-diagnostic-freeze-v3

That stdout was 164 bytes with SHA-256:

    a64dec2a14b7be5d8da218f86a4c438fbbbc0273f8d9979f9c38b2ade6e1cdbf

The query is consumed and may never be repeated. It established only the two
public ref values at that observation; it did not validate the later execution.
The repository remained PUBLIC. No visibility mutation or visibility query
occurred during O8v3 construction or execution.

## 3. Complete consumed O8v3 execution evidence

O8v3 used the execution root:

    /var/tmp/arc3-crosslevel-voi-action-qbc-v8-open

The root and authority clone are owner-controlled mode `0700`, uid/gid 1000.
The authority remains a clean detached O8v3 checkout with tree
`7d38de8f5cec16cab92c9d3b757a218e8e490272`, no remote, and exactly the seven
registered local Git configuration entries. The ten bootstrap steps and the
detached open-freeze verification completed once. The latter produced its exact
500-byte record with SHA-256:

    b75fe9fcaa1d5d0ae30d949f682ab3bf1c5fa8628563134a8cf054a8efc57e39

Preparation completed once with exit code 0 and empty stdout/stderr. Its
canonical receipt has:

    path:       /var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/preparation-receipt.json
    mode:       0444
    byte count: 89266
    SHA-256:    b3540fa1bf09dd6303bdef36001faa475002b9a03128a3602b19535689de026b
    status:     prepared
    attempts:   1
    ledger:     100 rows = 46 authority + 54 attempt

Its command-ledger SHA-256 is:

    caecece5ff0c39b44c9e67e447c38dae32746e5fe34344a91556bfd7465c662e

Its preparation-command-environment SHA-256 is:

    6bd8625e7d3f4f329785adacc96569c2aece8682cfbfb998fed96be17a6c8398

Both process clones were clean detached O8v3 trees. Their registered Python was
3.12.13, uv was 0.11.28, and their resolved Python executable SHA-256 was the
same. Neither process output parent contained a payload.

Independent preparation verification completed once with exit code 0 and empty
stderr. Its canonical receipt has:

    path:        /var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/preparation-verification.json
    mode:        0600
    byte count:  2961
    file SHA-256: 6173dc587efc3d0a1137920ef49885a2b7c4b7cac81288f3c879c4074bda2ff3
    content SHA-256: 5fa6e2ec22a6f00dba2efb7f2bbc2c39c5fa275bca017084ba6206cb83e80eec
    status:      verified

Its stdout was those file bytes plus one LF: 2962 bytes with SHA-256
`467475d50d46e4b9becb33d3112a10896b8e796d82e1d01159515bfafee52138`.

The sole Windows supervisor then completed with exit code 0 and empty outer
stdout/stderr. It created all four plain canonical ASCII artifacts:

| Artifact | Bytes | SHA-256 | Terminal fact |
| --- | ---: | --- | --- |
| lifecycle claim | 804 | `2987fbf1ddc5cbf2627cfade7aafea2251469795be0e2ff525da8ddc8758c253` | claim exists |
| verifier-start claim | 488 | `6fbdfd70d415054bae9f67272f2f0b65dd4ce5873efef5dee46b2e541f098755` | verifier started |
| remote receipt | 2419 | `2f0098f7c85329aad149bdb99293f975e85ee35a8e45aab46d6dcf60072fa862` | `failed`, attempts `[]`, selected attempt `null` |
| supervisor receipt | 1091 | `6d14c0fe64a7fa7657060abc769c0f272d7b5f03b54338459aa97d3a0b6bacb5` | `completed`, `verifier_completed`, verifier exit 1 |

The Windows files are owned by `MSI\User`, are non-reparse regular files, have no
final LF, and are exactly cross-hash-linked. The supervisor captured exact empty
child stdout and stderr, did not time out, and recorded null cleanup because the
verifier completed normally. A completed supervisor is not a successful remote
verification: the remote receipt is explicitly failed and contains no remote
attempt evidence.

Because the lifecycle claim existed, the frozen rule required exactly one Linux
lifecycle invocation regardless of the remote failure. That invocation copied
the four Windows artifacts byte-identically into the execution root, each as an
owner-controlled mode-`0444` immutable file. It also created:

| Linux evidence | Mode | Bytes | SHA-256 | Fact |
| --- | ---: | ---: | --- | --- |
| arm receipt | `0444` | 1083 | `0e34660892ac03f8bd52fe6713ea5e737731559fdaf2533684aac20822e32544` | status `failed` |
| lifecycle driver claim | `0600` | 484 | `5d8b75eb28d0bf2397c1a8a8504999da6fb43a1ea2b94ed9468d233d986b94d4` | exact driver argv hash |
| lifecycle ledger | `0600` | 1136 | `87d89b6371cc3dc7736c469632010ed1f4051e165ae0dc88165ca842491711f0` | stage `preparation_receipt_invalid` |
| emergency result bundle | `0600` | 2737 | `097c92383e19a91cc2447afbcca3946cae16690743783863b29eb4a2aa21584c` | administrative terminal |

The arm receipt links the preparation receipts and all four copied remote
artifacts. The ledger contains only `arm_returned`, arm exit 1, and null process
A/B execution fields. Its driver argv SHA-256 is:

    85d0f286790a6a651c58d461ffdb3f20ca9ee8c40709d16222aba8473747e88e

The emergency bundle is canonical and has content SHA-256:

    f85469430d44af3a7f557fc293fe260cc6fe43b3a61a85467d3268ce895c94ed

It records disposition `administrative_terminal`, stage
`finalizer_process_failed`, underlying stage `preparation_receipt_invalid`,
finalizer classification `nonzero`, exit code 2, `timed_out=false`, null child
cleanup, no normal bundle, and all five authorization Booleans false. Its sole
embedded file is:

    path:       docs/action_qbc_v8_open_diagnostic_result.md
    mode:       100644
    byte count: 891
    SHA-256:    2894e4767af22677f014a42fd2f370bf96c529334185920fbe014dad809d9e45
    Git blob:   1f8a3573f2061b1f91da5311576b895da8588562

An in-memory Git-object reconstruction gives the hypothetical result-v3 commit
`0a4e91da424ec47ce381301544f0801187dab1d5` for those unchanged bytes. This is a
deterministic arithmetic identity only. No such object or ref was created.

The in-lifecycle publisher returned nonzero. The later first registered
publication-only invocation refused with the exact error
`observed publish argv differs from registration`. Publication-only is
nominally repeatable, but section 6 proves every exact invocation must reproduce
that refusal. It may not be invoked again under O8v3.

The sole Linux lifecycle launcher returned host exit code 1 after approximately
5.1 seconds. The tool retained only this combined presentation, not raw
stdout-versus-stderr attribution:

    {"error":"registered result publisher failed: nonzero","status":"refused"}

No raw combined-stream hash was captured, so none is asserted. The later
publication-only refusal was observed once; no second recovery invocation was
made.

At the P8v7 correction freeze:

- the old execution root and all ten evidence files above remain byte- and
  mode-identical;
- no chmod, cleanup, replacement, retry, or evidence copy was performed;
- process A and B have no start claim, validator claim, validation receipt, or
  payload, and both output parents remain empty;
- no scientific module was imported and no scientific command started;
- `finalization-bundle.json`, `result-git-owner.json`, and `result-git-work` are
  absent;
- the authority and original repositories have no result branch, result-v3 tag,
  or result-v3 ref; and
- the authority and original O8v3 worktrees remain clean.

## 4. Deterministic failure one: verifier snapshot self-invalidation

The Windows verifier captured the registered repository ancestor snapshot before
publishing its verifier-start claim. The registered ancestor chain includes:

    D:\
    D:\kaggle competitions
    D:\kaggle competitions\arc3-crosslevel-voi

The verifier-start claim is a sibling of the repository under
`D:\kaggle competitions`. Publishing it therefore changes metadata of an
ancestor already captured in the snapshot. The later repository-identity check
recaptures the chain and requires exact equality, so the verifier deterministically
invalidates its own snapshot before `_run_attempts`.

The broad exception handler deliberately reduced that pre-attempt exception to
`attempts=[]`, `selected_attempt=null`, and `status=failed`; the exact exception
text was not retained. This document does not invent it. The source ordering,
registered paths, and observed zero-attempt receipt establish the administrative
self-invalidation. No remote query attempt was recorded.

O8v4 must publish and validate the exclusive verifier-start claim first, then
capture the repository snapshot, then perform the read-only repository identity
commands and unchanged-snapshot comparison. The snapshot must still cover the
same registered repository root, administrative tree, config, exclude, index,
refs, packs, hooks, and ancestor chain. Moving the capture does not weaken any
identity check; it merely places the verifier's own authorized sibling write
before the frozen before/after interval.

No parent-directory mutation is ignored, normalized away, or tolerated inside
the new interval. No compatibility order or fallback is accepted.

## 5. Deterministic failure two: immutable-mode reader mismatch

Preparation deliberately publishes its receipt mode `0444`. Arm deliberately
copies the four Windows artifacts as mode `0444` and publishes its own receipt
mode `0444`. Arm accepted those immutable modes and returned status `failed`
because the canonical copied remote receipt itself recorded `status=failed`,
zero attempts, and no selected attempt. After arm returned, the lifecycle's
separate blanket evidence reader required mode `0600` and independently
classified the valid mode-`0444` preparation receipt as `unsafe_type`. The
lifecycle ledger therefore recorded `preparation_receipt_invalid` before
science. The corresponding finalizer/publisher evidence readers would likewise
reject each copied immutable remote artifact and the immutable arm receipt when
encountered through their blanket mode-`0600` paths.

O8v4 freezes this exact role-to-mode partition:

Immutable evidence, exact mode `0444`:

    preparation receipt
    copied remote lifecycle claim
    copied remote verifier-start claim
    copied remote verification receipt
    copied remote supervisor receipt
    arm receipt

Private local evidence, exact mode `0600`:

    preparation-verification receipt
    lifecycle driver claim
    lifecycle ledger
    process A and B start claims
    process A and B validator claims
    process A and B validation receipts
    process A and B payload files
    normal finalization bundle
    emergency result bundle
    result-Git owner claim

O8v4 must use one centralized, exhaustive expected-mode map for all lifecycle,
finalizer, renderer, bundle-validator, and publisher evidence reads. Each read
must select a registered role and require exactly that role's mode. A path not in
the map, a duplicate role, a role/path mismatch, a mode outside its singleton,
or a nonregular, symlinked, wrong-owner, multiply linked, changing, or oversized
file fails closed. Mode `0444` is not broadly accepted, and mode `0600` is not a
fallback for an immutable role.

The already-correct preparation publisher/reader and arm copier/publisher, plus
the corrected lifecycle reader, finalizer, renderer, selected-bundle validator,
and result publisher, must share and test the same partition. No existing O8v3
file may be chmodded to simulate the fix.

## 6. Deterministic failure three: timeout-wrapper argv mismatch

The O8v3 registration stores this exact 23-token result-publisher argv:

    ["/usr/bin/timeout","--signal=TERM","--kill-after=5s","600s","/usr/bin/python3","-I","-B","scripts/execute_action_qbc_v8_open_lifecycle.py","publish","--repository-root",".","--registration","artifacts/action_qbc_v8_open_registration.json","--driver-claim","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/lifecycle-driver-claim.json","--lifecycle-ledger","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/lifecycle-ledger.json","--finalization-bundle","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/finalization-bundle.json","--emergency-bundle","/var/tmp/arc3-crosslevel-voi-action-qbc-v8-open/emergency-result-bundle.json","--control-time-seconds","570"]

Its canonical argv SHA-256 is:

    b0ba4bc25be11a70234997f75eddc6a4b8fc6c17f00a287c614e4285bb18e053

GNU timeout consumes the first four wrapper tokens and execs Python. The
publisher entry point can therefore reconstruct only the exact 19-token suffix,
starting `/usr/bin/python3`, while the registration validator compared that
child-visible suffix directly with all 23 registered tokens. The separate source
prefix check also requires the child-visible array to begin Python, `-I`, `-B`,
and the script path. No invocation can satisfy both frozen comparisons.

O8v4 retains one full registered timeout-wrapped publisher argv. It must
independently require its exact four-token prefix:

    ["/usr/bin/timeout","--signal=TERM","--kill-after=5s","600s"]

The lifecycle child launcher continues to validate and execute the full array.
The publisher entry point must compare its reconstructed child-visible argv only
to the exact registered suffix beginning at token 4. The suffix is derived from
the one registered full array; no second operator-selected argv, compatibility
alias, inferred wrapper, or schema field is introduced. Any altered prefix,
suffix, duration, kill-after value, script, argument, or ordering fails closed.

The exact O8v3 attempted full array was token-for-token equal to its registration;
the refusal was not an operator quoting or launcher error.

## 7. Fresh namespaces, stable schemas, and scientific invariance

O8v3's claimed paths are permanent evidence and may not be reused. O8v4 uses the
fresh execution root:

    /var/tmp/arc3-crosslevel-voi-action-qbc-v8-open-v4

All authority, process, output, preparation, verification, copied-remote, arm,
lifecycle, bundle, result-owner, and result-work paths are derived beneath that
new root exactly as incorporated, with only the root substitution.

O8v4 uses exactly these four fresh Windows paths:

    D:\kaggle competitions\arc3-crosslevel-voi-action-qbc-v8-remote-verification-claim-v4.json
    D:\kaggle competitions\arc3-crosslevel-voi-action-qbc-v8-remote-verifier-start-claim-v4.json
    D:\kaggle competitions\arc3-crosslevel-voi-action-qbc-v8-remote-verification-v4.json
    D:\kaggle competitions\arc3-crosslevel-voi-action-qbc-v8-remote-verification-supervisor-v4.json

Their Linux source paths are the exact `/mnt/d/kaggle competitions/...-v4.json`
counterparts. All fresh roots and paths must be absent before O8v4 bootstrap or
claim. Existing, unsafe, ambiguous, nonempty, or partially created fresh state
stops permanently; it is not cleaned or retried. The old unsuffixed Windows
files and old execution root are never read as O8v4 inputs.

The registration schema remains `action-qbc-v8-open-registration-v1`. Every
incorporated receipt, claim, payload, bundle, and result-document schema name and
exact key set remains unchanged. The execution contract retains its exact
70-key shape. The expected-mode map and publisher suffix relation are frozen
administrative implementation invariants, not new JSON fields.

The treatment ID, diagnostic ID, platform, comparison, transform, resource,
payload, result-document, and scientific-contract identifiers remain unchanged.
All 140 scientific rows, twelve public scenes, transforms, roles, controls,
selectors, data, tolerances, reasons, fallbacks, scientific resource counters,
analysis rules, dependencies, scientific functions, payload limits, and
2,100/2,400/2,700-second scientific limits remain exactly as incorporated.

All five authorization Booleans remain false. This document authorizes no
lockbox, sealed evaluation, private-scene access, gameplay, leaderboard
submission, model change, positive mechanism claim, or third scientific start.
O8v4 is a fresh administrative execution attempt of the same unchanged
treatment, not permission to tune from O8v3's pre-science failure.

Registration content/file hashes, preregistration-tree manifest, O8v4
added-file manifest, source identities, path-derived argv hashes, and the Linux
tool/path identities are regenerated from replacement bytes. Changed
administrative identities are not scientific or named-schema changes.

The exact argv-hash key partition is frozen. These hashes must derive-change:

    arm
    bootstrap
    finalizer
    lifecycle_driver
    payload_validator
    post_preparation_validation
    preparation
    producer
    remote_supervisor
    remote_verifier
    result_publisher
    result_ref_transaction
    scientific

These hashes must remain unchanged:

    environment_build
    linux_host_launcher
    preflight
    reconstructor
    tests

No other argv-hash key may be added, removed, renamed, or changed.

## 8. Exact P8v7 reset and O8v4 tree boundary

P8v7 deletes all and only these fourteen O8v3 non-registration paths:

    docs/action_qbc_v8_open_diagnostic_runbook.md
    scripts/build_action_qbc_v8_open_registration.py
    scripts/execute_action_qbc_v8_open_lifecycle.py
    scripts/finalize_action_qbc_v8_open_diagnostic.py
    scripts/prepare_action_qbc_v8_open.py
    scripts/reconstruct_action_qbc_v8_open_registration.py
    scripts/run_action_qbc_v8_open_diagnostic.py
    scripts/supervise_action_qbc_v8_remote_tag.py
    scripts/validate_action_qbc_v8_open_payload.py
    scripts/verify_action_qbc_v8_remote_tag.py
    src/arc3_voi/action_qbc_v8_audit.py
    tests/test_action_qbc_v8_audit.py
    tests/test_action_qbc_v8_lifecycle.py
    tests/test_action_qbc_v8_registration.py

P8v7 also deletes the O8v3 registration:

    artifacts/action_qbc_v8_open_registration.json

Together with the sole P8v7 document addition, those deletions are P8v7's exact
direct D15+A1 diff from O8v3. The net P8v6-to-P8v7 diff is exact A1.

After P8v7 is committed, tagged, and successfully published, O8v4 re-adds all
and only the fourteen non-registration paths above. Before the producer runs,
they are the only staged paths, each stage 0 and mode `100644`. Their differences
from O8v3 are limited to the three corrections in sections 4-6, fresh paths,
lineage/ref identities, runbook text, regenerated administrative hashes, and
required regression tests. Every scientific source and semantic must reverse to
the same frozen v7 source under the incorporated inverse audit.

The registration builder runs once, after those fourteen additions are staged,
with:

    ["uv","run","--frozen","--extra","dev","python3","-I","-B","scripts/build_action_qbc_v8_open_registration.py","--repository-root",".","--preregistration-tag","prereg-action-qbc-v8-open-bounded-remote-verification-v7","--output","artifacts/action_qbc_v8_open_registration.json"]

It creates the sole fifteenth addition:

    artifacts/action_qbc_v8_open_registration.json

The registration is a no-follow regular worktree file owned by the registered
Linux user, mode `0600`, canonical JSON without final LF, and staged Git mode
`100644`. Reconstruction must be byte-identical. Only after every incorporated
producer, reconstructor, pytest, Ruff, strict-mypy, source-transformation,
raw-byte, tool, environment, no-science, and post-commit gate passes may the
fifteen additions be committed as O8v4, P8v7's direct child, and tagged
freeze-v4.

Any failed, partial, timed-out, ambiguous, or repeated one-shot construction or
gate is not O8v4 and requires another prospective preregistration.

## 9. Authority and attempt command plans

The canonical O8v4 authority plan contains exactly 54 argv rows, in this order:

1. the incorporated initial config query;
2. the active freeze-v4 tag-type, tag-resolution, and HEAD rows;
3. the four incorporated rows for each of P8v1, P8v2, P8v3, and P8v4: tag type,
   tag resolution, sole-parent relation, and registered diff;
4. four historical O8v1 rows: freeze-v1 tag type, tag resolution, sole parent
   P8v4, and exact P8v4-to-O8v1 A15 diff;
5. four historical P8v5 rows: v5 tag type, tag resolution, sole parent O8v1, and
   exact O8v1-to-P8v5 D15+A1 diff;
6. four historical O8v2 rows: freeze-v2 tag type, tag resolution, sole parent
   P8v5, and exact P8v5-to-O8v2 A15 diff;
7. four historical P8v6 rows: v6 tag type, tag resolution, sole parent O8v2, and
   exact O8v2-to-P8v6 D15+A1 diff;
8. four historical O8v3 rows: freeze-v3 tag type, tag resolution, sole parent
   P8v6, and exact P8v6-to-O8v3 A15 diff;
9. four P8v7 rows: v7 tag type, tag resolution, sole parent O8v3, and exact
   O8v3-to-P8v7 D15+A1 diff;
10. the active P8v7 document `ls-tree` and `cat-file` rows;
11. the O8v4 sole-parent P8v7 row and exact P8v7-to-O8v4 A15 diff row; and
12. the incorporated post config, HEAD, tree, batch-object, index, and status
    rows.

The P8v6-to-P8v7 net-A1 relation is independently validated even where it is
not a separate authority-command row. All producer and consumer implementations
must generate the same byte-identical 54-row order.

The preparation-attempt command plan remains exactly 54 rows for each permitted
preparation attempt, with at most two preparation attempts under the incorporated
cleanup and atomic-promotion rules. Its commands, ordering, timeouts, caps,
cleanup, and evidence semantics remain unchanged except for declared v4 path
substitutions. The remote verifier's separate maximum of three internal remote
attempts remains incorporated but is not part of this 54-row plan. Neither kind
of internal attempt is a retry of O8v3's zero-attempt receipt or anonymous check.

A successful one-attempt O8v4 preparation receipt therefore contains exactly
108 command-ledger rows: the 54-row authority plan followed by the one complete
54-row attempt plan. A failed first preparation attempt may use only the
incorporated bounded-prefix semantics; no successful one-attempt receipt may
contain fewer, additional, reordered, or duplicated rows.

## 10. Existing PUBLIC state and recovery publication

The repository is already PUBLIC. This recovery authorizes no `gh repo edit`,
browser visibility mutation, API visibility mutation or query,
PUBLIC-to-PRIVATE transition, second PRIVATE-to-PUBLIC transition, or visibility
rehearsal.

After its complete local commit audit, publish P8v7 with exactly one non-force
atomic Windows Git invocation:

    ["C:\\Users\\User\\anaconda3\\Library\\bin\\git.exe","--no-replace-objects","--no-optional-locks","-C","D:\\kaggle competitions\\arc3-crosslevel-voi","push","--atomic","origin","refs/heads/action-qbc-v8-prereg:refs/heads/action-qbc-v8-prereg","refs/tags/prereg-action-qbc-v8-open-bounded-remote-verification-v7:refs/tags/prereg-action-qbc-v8-open-bounded-remote-verification-v7"]

After O8v4 and every post-commit audit pass, publish O8v4 with exactly one
non-force atomic Windows Git invocation:

    ["C:\\Users\\User\\anaconda3\\Library\\bin\\git.exe","--no-replace-objects","--no-optional-locks","-C","D:\\kaggle competitions\\arc3-crosslevel-voi","push","--atomic","origin","refs/heads/action-qbc-v8-prereg:refs/heads/action-qbc-v8-prereg","refs/tags/action-qbc-v8-open-diagnostic-freeze-v4:refs/tags/action-qbc-v8-open-diagnostic-freeze-v4"]

Each push is single-use. Failure, partial or non-atomic behavior, uncertainty, or
unexpected ref movement stops before the next boundary. No ref is forced,
deleted, rewound, repointed, or reused.

No remote query, browser refresh used as a protocol check, API query,
authenticated ref query, SSH query, anonymous query, connectivity probe, or
visibility action may occur between the P8v7 push and O8v4 push. The next
protocol network action after successful O8v4 publication is exactly one
credential-disabled anonymous query from the registered neutral WSL cwd and
empty environment for only:

    refs/heads/action-qbc-v8-prereg
    refs/tags/action-qbc-v8-open-diagnostic-freeze-v4

Success requires exactly those two records, each once at the recorded lowercase
40-hex O8v4 commit, with empty stderr and no peeled, duplicate, additional,
malformed, credential-assisted, or authenticated record. The query is consumed
whether it succeeds or fails and is never retried. Only exact success authorizes
fresh-root bootstrap.

## 11. O8v4 downstream lifecycle and result boundary

After the sole O8v4 anonymous check succeeds, the incorporated bootstrap,
preparation, independent preparation verification, Windows supervisor, Linux
lifecycle, finalizer, publisher, transfer, and result-verification protocol runs
once in the fresh namespaces from section 7.

The Windows verifier must create its fresh lifecycle claim and verifier-start
claim exactly once. Claim existence again makes the Linux lifecycle mandatory
regardless of verifier or supervisor outcome. A verified receipt authorizes the
unchanged scientific sequence. A valid failed remote receipt skips science and
must still reach a publishable administrative result. No missing error string or
zero-attempt failure may be described as successful remote verification.

The corrected evidence-mode map must allow the immutable 0444 artifacts to
reopen with their exact hashes while keeping every private-local artifact 0600.
The corrected publisher must accept only the 19-token child suffix derived from
the registered 23-token wrapper, while every parent/operator invocation retains
the full registered timeout wrapper.

If R8v4 is validly created, the authority contains only the lightweight tag:

    refs/tags/action-qbc-v8-open-diagnostic-result-v4

The authority result branch remains absent. Only after independent commit,
parent, tree, delta, document, and tag validation may the original repository
receive:

    refs/heads/action-qbc-v8-open-diagnostic-result
    refs/tags/action-qbc-v8-open-diagnostic-result-v4

Both must resolve to the same R8v4 before the two registered non-force result
pushes and the sole final two-ref verification. Result-v3 remains absent. PUBLIC
visibility remains unchanged through the complete lifecycle and publication.

## 12. Required implementation and regression gates

Before O8v4, all incorporated offline tests and exact frozen gates remain
mandatory. Additional tests must prove:

- O8v3 is P8v6's exact A15 child at commit, tree, and freeze-v3;
- P8v7 is O8v3's exact D15+A1 child, its tree equals P8v6 plus only this
  document, and P8v6-to-P8v7 is net A1;
- O8v4 is P8v7's exact A15 child and freeze-v4 is a lightweight tag only at
  O8v4;
- the 54-row authority plan and 54-row attempt plan are byte-identical across
  producer, reconstructor, lifecycle, finalizer, runner, validators, and tests;
- all O8v3 external artifacts retain the exact bytes, modes, owners, hashes,
  statuses, cross-links, and absence facts in section 3, and no old path is an
  O8v4 input;
- the old result-v3 branch/tag, result owner/work, normal bundle, process claims,
  validations, outputs, and scientific starts remain absent;
- all O8v4 Linux and Windows paths are fresh, exact, absent before use, and
  reject every unsuffixed O8v3 path;
- a real start-claim publication precedes repository snapshot capture, a
  following unchanged identity pass succeeds, and an admitted remote attempt
  produces a nonempty attempt ledger without using the network in tests;
- any ancestor mutation after the new snapshot still fails closed;
- the centralized expected-mode map is exhaustive and identical for prepare,
  arm, lifecycle, finalizer, renderer, bundle validation, and publisher;
- every immutable role above accepts only exact `0444`, every private-local role
  accepts only exact `0600`, and wrong-mode, unknown-role, alias, symlink,
  wrong-owner, hard-link, changing, or oversized evidence is rejected;
- a real prepare-published mode-0444 receipt passes the lifecycle reader and no
  longer yields `preparation_receipt_invalid`;
- copied remote artifacts and the arm receipt pass sequential reopen checks at
  mode `0444`;
- a real subprocess integration test spans producer publication, arm copying,
  arm receipt publication, and lifecycle/finalizer reopening for the complete
  `0444`/`0600` role matrix rather than calling only unit helpers;
- the full 23-token publisher array has the exact timeout prefix, its derived
  child suffix has exactly 19 tokens, and only that suffix passes entry-point
  observation;
- altered wrapper, duration, kill-after, interpreter, flags, script, argument,
  order, or direct full-array child observation is rejected;
- real cross-process tests exercise both the in-lifecycle timeout wrapper and
  the standalone repeatable wrapper through publisher `main()`, not only mocked
  inner helpers;
- a valid emergency bundle and a valid normal bundle each publish idempotently
  to only the registered result-v4 tag, with the authority result branch absent;
- a canonical remote-failure fixture reaches `remote_verification_failed`, skips
  both scientific starts, finalizes administratively, and publishes R8v4;
- a verified remote fixture reaches the unchanged two-process scientific path
  exactly once, subject to all incorporated gates;
- the registration retains its v1 schema, exact 70-key execution contract, and
  every named receipt/claim/payload/bundle key set;
- a successful one-attempt preparation contains exactly 108 command rows, and
  the exact changed and unchanged argv-hash key sets in section 7 are enforced;
- every O8v3-to-O8v4 executable difference is a declared administrative
  correction, path/ref/lineage substitution, regenerated identity, runbook
  change, or regression test, and the scientific inverse audit remains exact;
- the branch permits only O8v3 -> P8v7 -> O8v4 fast-forwards and each publication
  argv contains only its paired immutable tag;
- already-PUBLIC state permits no visibility mutation/query, and the sole O8v4
  anonymous argv accepts only branch plus freeze-v4 once at O8v4; and
- all tests remain offline, create no real old or fresh execution root or claim,
  mutate no visibility or remote ref, import no scientific module on a
  pre-start failure, and write no real result path.

The builder runs only after the fourteen authored additions are staged.
Registration reconstruction must be byte-identical. Full frozen pytest, Ruff,
and strict-mypy gates run once, in order, in the incorporated explicit Ubuntu
environment. A failed, timed-out, ambiguous, or repeated one-shot gate requires
another preregistration.

## 13. Claims and actions that remain prohibited

This recovery records three administrative cross-component failures discovered
only after an irreversible claim. It is not evidence about the Action-QBC
mechanism, model quality, scientific rows, public scenes, hidden generalization,
causality, calibrated inference, runtime efficacy, security, or Kaggle
performance. The absence of a scientific start is a protocol fact, not a null
scientific result.

O8v3's successful anonymous publication check proves only that its two requested
refs were publicly readable. The later zero-attempt remote receipt proves no
remote query occurred. Neither is cryptographic attestation, proof against a
malicious same-authority actor, or evidence of scientific execution.

No operator may retry O8v3 lifecycle, arm, finalizer, verifier, supervisor,
publisher, anonymous query, or result publication; invoke an unwrapped
publisher; manually construct or transfer the hypothetical result-v3 commit;
create a result-v3 ref; chmod or copy old evidence to make it readable; reuse an
old claim/root/path; edit a frozen O8v3 source; or describe the valid emergency
bundle as a published result.

No failed or incomplete O8v4 replacement may be tuned away, cleaned, retried,
or described as scientific evidence. It remains publishable only with its exact
registered administrative or scientific meaning.
